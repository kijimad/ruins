// SteamPipe depot 定義。Makefile が @...@ を実値へ展開する。テンプレートなので直接編集しない。
// content/ 配下すべてを depot 直下へ写す。中身は Linux/Windows の2実行体。
"DepotBuild"
{
	"DepotID"     "@DEPOT_ID@"
	"ContentRoot" "@CONTENT@"

	"FileMapping"
	{
		"LocalPath" "*"
		"DepotPath" "."
		"Recursive" "1"
	}
}
