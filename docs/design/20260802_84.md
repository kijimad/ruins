---
status: draft
tags: [ui, item]
auto: needs-decision
---

# メニューをタブ化して手数を減らす —— 動詞タブ・画面タブ・1カラム統一

## 背景

現状のアイテム操作は導線が深く、よく使う操作ほど手数がかさむ。

- **アイテムを1つ使うまでに確定 Enter が3回、階層が3層**。`M`→ダンジョンメニュー→「所持」→アイテム選択→アクションウィンドウで動作選択、という流れになる。`internal/states/dungeon_input.go:34` の `M` から `NewDungeonMenuState` を push し、そこから `NewInventoryMenuState`、さらにアイテム選択後に `invSubStateWindow` のアクションウィンドウが必ず挟まる、`internal/states/inventory_menu.go:187`。
- **インベントリ直開きのホットキーが未配線**。`ActionOpenInventory` は `internal/inputmapper/action.go` に定義があり `DoAction` の分岐もあるが、`DungeonState.HandleInput` から生成されないため、必ず `M`→「所持」の中継を踏む。
- **アイテム選択後に必ずアクションウィンドウが挟まる**。消費アイテムでも「使う/捨てる/閉じる」から選び直す、`internal/states/inventory_menu.go:468` の `getActionItems`。
- **動詞ごとに導線とキーがバラバラ**。拾う=`G`、置く=`P`、使う/読むは `M` 経由、装備は別メニュー、と1つのアイテムに対する操作の一覧性が無い。
- **食べる/飲むは「使う」に吸収、投げる・アイテムの調べるは未実装**。`internal/activity/use_item.go` が栄養も回復もまとめて処理し、ログだけ「食べた。」に分かれる。`ThrowActivity` は存在しない。

先行事例の Elona は**動詞先行**、動詞キーを押すとその動詞に使えるアイテムだけの一覧が出る、で手数が短い。本 doc はこれを参考にしつつ、**動詞を上部タブにして戻らず切り替えられる**形へ拡張する。

## 要件

- **動詞タブ画面**。ダンジョン・各メニューから、動詞ショートカット1キーで対応タブへ直達する。上部タブ、
  調べる/置く/食べる飲む/読む/使う/投げる、を戻らずに切り替えられる。各タブはその動詞に使えるアイテムだけを
  表示し、Enter で即実行する。中間のアクションウィンドウを廃す。
- **画面タブメニュー**。装備・スキルを同じタブ UI でまとめる。こちらはダンジョンからの直ショートカットは持たず、
  メニューから開いてタブで切り替える。所持アイテムへの動詞でなく、キャラクター情報の閲覧・操作が対象。
- **1カラムに統一**。既存の2カラム、左右分割、レイアウトをやめ、タブ画面は縦1カラムにする。
- タブ帯は動詞タブと画面タブで共有する部品として抽出する。
- 既存基盤を再利用する。タブは `hooks.UseTabMenu`、`internal/hooks/tabmenu.go`、と `styled` の行部品、実行は
  既存の `activity` 機構。車輪の再発明をしない。
- **移動を WASD 廃止しカーソルキーのみにする**。英字を動詞コマンドへ全面的に空ける。斜め移動は Shift + 矢印。
  これで置く `D`・食べる `e` などの英字動詞と移動が衝突しなくなる。

## 設計

### 動詞タブ画面

動詞タブ画面を担う state を1つ足す。`StatusState`、`internal/states/status_state.go`、が5タブの雛形として最も近く、これに倣う。

- 上部に動詞タブ帯を置く。`styled.NewRowContainer` に各動詞を `styled.NewListItemText(label, color, isSelected, res)` で横並びにする。カーソル管理は `hooks.UseTabMenu(store, key, cfg)`。
- 各タブが1動詞に対応し、バックパックのアイテムを**その動詞の `accept` 述語で絞って**表示する。空なら「該当なし」を出す。
- アイテム選択の Enter で**選択アイテムへ動詞を即実行**する。従来の「使う/捨てる/閉じる」を選び直すアクションウィンドウは持たない。
- タブ切替は左右キーと `Tab`/`Shift+Tab`、`internal/states/window.go` の `HandleMenuInput` が既に `ActionMenuTabNext/Prev` を返す。ショートカットキーは画面を開くと同時に対応タブへジャンプし、開いている間も同じキーでタブを移せる。

### 動詞レジストリ —— 表駆動で拡張点を1つに

動詞を表駆動の登録リストにする。散布の feature 一覧やカタログと同じ発想で、動詞の追加を「エントリ1つ足す」ことに還元する。

```go
// itemVerb は動詞タブ1つ分の定義。Accept で対象アイテムを絞り、Exec で選択アイテムへ動詞を適用する。
// Exec は即時 activity か、ターゲット選択の別 state への遷移のどちらかを返す。
type itemVerb struct {
	ID     verbID        // "examine" / "place" / "consume" / "read" / "use" / "throw"
	Label  string        // タブ見出し。調べる 等
	Key    verbKey       // 直達ショートカット。下記キーマップ参照
	Accept func(w.World, ecs.Entity) bool
	Exec   func(w.World, ecs.Entity) (es.Transition[w.World], error)
}

// verbs は表示順に並べた動詞タブの一覧。タブ順とショートカットの対応表を兼ねる。
func verbs() []itemVerb { ... }
```

`Accept` は既存のコンポーネント述語を流用する。

| 動詞 | 対象の絞り込み `Accept` | 実行 `Exec` | 実装状況 |
|---|---|---|---|
| 調べる | 全アイテム | 詳細ポップアップ表示 | 新規、軽量。`Description` は既存 |
| 置く | バックパック全アイテム | 既存 `PlaceState` のタイル選択へ | 既存流用、`internal/states/place_state.go` |
| 食べる/飲む | `Consumable` かつ `ProvidesNutrition` または `ProvidesHealing` | 既存 `UseItemActivity` | 既存 |
| 読む | `Book` を持つ | 既存 `ReadActivity` | 既存 |
| 使う | `Consumable` かつ栄養・回復を持たない道具 | 既存 `UseItemActivity` | 既存 |
| 投げる | `Throwable` を持つ | `ThrowActivity` + ターゲット選択 | 新規 |

捨てる、`DropActivity`、は動詞タブに出さない。足元へ落とす操作は「置く」がタイル選択で兼ねられるため、タブ数を絞る。装備は関心が別、`EquipMenu`、なのでこの画面には含めない。

### 食べる/飲む と 使う の振り分け —— 効果で推論する

食べると飲むは同じ操作にする。栄養も回復も `UseItemActivity` が処理し、動詞として分ける意味が無いためである。よって
食べ物・飲み物を区別する分類は持たず、1つの「食べる/飲む」タブにまとめる。

- `食べる/飲む` = `Consumable` かつ、`ProvidesNutrition` または `ProvidesHealing` を持つ。食料もポーションもここに入る。
- `使う` = `Consumable` かつ、栄養も回復も持たない道具。offensive な消費物などが該当し、無ければ空タブになる。
- 効果適用の実体は `UseItemActivity` のままで、動詞は入口の絞り込みだけで分ける。食べる/飲むの区別が消えたので効果から
  の推論で曖昧さは出ない。分類コンポーネント `ConsumeKind` は不要で、データ変更を伴わない。ログ文言は効果の有無で選ぶ。

### 投げる —— 新規

- `Throwable` マーカーを足し、`Accept` で対象を絞る。
- `ThrowActivity` を `internal/activity/throw.go` に足し、`BehaviorName` へ登録する、`internal/components/activity.go` の一覧。
- 対象タイル・方向の選択は `PlaceState` のタイル選択フェーズと同型の UI を使う。命中時に `InflictsDamage` 等の効果を適用する。ポーションが割れて範囲効果、などは後続に回す。

### キー割り当てと衝突の解消

動詞キーは 調べる `X` / 置く `D` / 食べる飲む `e` / 読む `r` / 使う `t` / 投げる `T`。移動を WASD 廃止しカーソル
キーのみにするので、英字はすべて動詞へ空く。

| 動詞 | キー | 備考 |
|---|---|---|
| 食べる/飲む | `e` | 空き。食べる飲むは1タブに統合 |
| 読む | `r` | 空き |
| 使う | `t` | 空き |
| 投げる | `T` | `t` と物理同一。`KeyT` + `KeyShift` で区別。前例は `window.go:28` の `Tab`/`Shift+Tab` |
| 置く | `D` | 移動を WASD から外すので空く |
| 調べる | `X` | 現行は `ActionOpenFieldInfo`。フィールド情報を別キー、例 `l`、へ移し `X` を調べるに充てる |

- **移動は WASD 廃止・カーソルキーのみ**。現在ダンジョンの移動は `WASD` と矢印の両対応で、斜めは `Shift` 併用。
  WASD を移動から外して英字を全面的に動詞へ空ける。移動は矢印、斜めは `Shift` + 矢印にする。メニュー操作の
  `HandleMenuInput` も WASD をやめ矢印に寄せて一貫させる。
- `t`/`T` は物理キーが同じなので、`IsKeyJustPressed(KeyT)` と `IsKeyPressed(KeyShift)` の併用で分ける。これは
  コードベース既存の唯一の流儀、`Tab`/`Shift+Tab`。
- `X` はフィールド情報に使われている。調べるへ充てるならフィールド情報を別キーへ移す。フィールド情報も
  「調べる」の一種なので、将来は調べるタブへ統合する余地もある。
- 実装は各 state の `HandleInput` に分岐を足し、`DoAction` で動詞タブ画面を対応タブで push する。`window.go` に
  共通ヘルパを1つ置き、ダンジョンからも各メニューからも同じキーで開けるようにする。画面タブメニュー、装備・
  スキル、は直達キーを持たず、ダンジョンメニュー `M` から開く。

### 画面タブメニュー —— 装備・スキル

キャラクター情報の閲覧・操作を、動詞タブと同じタブ UI でまとめた画面タブメニューにする。タブは 装備・スキル。
所持アイテムへの動詞でなく、装備スロットやスキル一覧が対象なので、動詞タブ画面とは別 state にする。

- ダンジョンからの直ショートカットは持たない。ダンジョンメニュー `M` から開き、`Tab`/左右でタブを切り替える。
- 装備タブは既存 `EquipMenu` のスロット選択・着脱を1カラムのタブへ畳む。スキルタブはスキル一覧を表示する。
  現行 `StatusState` がスキルを描いているので、その表示を流用する。
- 将来ステータスなど他のキャラクター画面もこのタブへ足せる。動詞タブと同じ表駆動のタブ定義にする。

### 1カラムに統一

タブ画面は縦1カラムにする。現行 `StatusState` は左右2分割、`styled.NewVSplitContainer`、だが、タブで面を
切り替える方式では2カラムは情報が分散して追いにくい。タブ帯の下にアイテム/項目の縦リストを1列で置き、説明は
最下部の1行に集約する。既存の2カラムメニューも順次1カラムへ寄せ、レイアウトの一貫性を保つ。

### 共有タブ帯部品

動詞タブと画面タブが同じ見た目・操作になるよう、上部タブ帯を共通部品として抽出する。現状はタブ帯が各 state で
手書き、`status_state.go` の `buildCategoryContainer` など、なので、`styled` へ「タブ見出しの横帯」を1つ切り出し、
両画面から使う。カーソル状態は既存の `hooks.UseTabMenu` をそのまま使う。

### 既存メニューとの関係

- 動詞タブ画面は**動詞先行の即操作**に特化する。隊員別にアイテムを閲覧する `InventoryMenu` は関心が別なので
  当面残す。装備は画面タブメニューの装備タブへ移す。
- インベントリのアクションウィンドウ、使う/読む/分解/捨てる、は動詞タブ画面が主導線になった後に整理の余地が
  ある。まず併存させ、移行を見てから縮小する。

## 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/states/item_action_state.go`(新規) | 動詞タブ画面。共有タブ帯 + `UseTabMenu` + `Accept` 絞り込み + Enter 即実行。1カラム |
| `internal/states/character_state.go`(新規) | 画面タブメニュー。装備・スキルのタブ。直達キーなし。1カラム |
| `internal/widgets/styled/`(タブ帯) | 上部タブ帯の共通部品を抽出。動詞タブと画面タブで共有 |
| `internal/states/window.go` | 動詞ショートカットを各メニュー・ダンジョンから開く共通ヘルパ |
| `internal/states/dungeon_input.go` | ダンジョンの `HandleInput`/`DoAction` に動詞キーと遷移。移動を WASD 廃止しカーソルキーのみに |
| `internal/states/window.go`(メニュー操作) | `HandleMenuInput` の WASD をやめ矢印へ寄せる |
| `internal/inputmapper/action.go` | 動詞ごとの `ActionID`、調べる/置く/食べる飲む/読む/使う/投げる |
| `internal/components/`(throwable) | `Throwable` マーカー。登録表 + `make generate`。消費の分類は足さない |
| `internal/activity/throw.go`(新規) | `ThrowActivity` と `BehaviorName` 登録 |
| `internal/states/equip_menu.go` | 装備の機能を画面タブメニューの装備タブへ移す |
| `internal/states/status_state.go` ほか2カラムメニュー | 左右分割をやめ1カラムへ寄せる |
| `assets/metadata/entities/raw/` | 投擲物へ `Throwable` 付与 |
| `internal/states/golden_test.go` | 動詞タブ画面・画面タブメニューの VRT、各タブの表示、1カラム |
| `internal/states/action_handlers.go` ほか | `X` を調べるへ充てるためのフィールド情報キー移設 |

## 採用しなかった方法

- **置くを `P` 維持にして移動キーを触らない** —— 移動体系に手を入れずに済むが、動詞キーが記号と英字に割れて
  一貫しない。移動を WASD 廃止しカーソルキーのみにして英字を全面的に空け、置く `D`・食べる `e` 等を英字へ揃える。
- **消費アイテムに `ConsumeKind` を足して食/飲/使を厳密に分ける** —— 食べると飲むを別動詞にするなら分類が要るが、
  両者は同じ操作にするので不要。栄養・回復を持つ消費物を食べる/飲むへ、持たない道具を使うへ、効果で振り分ける。
  データ変更を伴わない。
- **`InventoryMenu` を廃してすべてこの画面へ寄せる** —— 隊員別の閲覧と動詞先行の即操作は関心が別。まず併存
  させ、移行を見てから整理する。
- **アイテム選択後にアクションウィンドウを挟む現行方式を維持** —— 中間1階層が毎回入り、手数短縮の要件に反する。
  動詞先行で入口の1回に畳む。
- **2カラムを維持する** —— タブで面を切り替える方式では左右分割は情報が散る。1カラムに統一して視線移動を減らす。
- **装備・スキルにも直達ショートカットを付ける** —— 戦闘中に即使う動詞と違い、装備・スキルは頻度が低く、英字の
  枯渇も招く。画面タブメニューはメニュー経由に留める。

## コメント

- Elona の動詞先行を、タブ横断で戻らず切り替えられる形へ拡張する。手数は「動詞キー→アイテム→実行」の実質
  1階層になる。
- 移動を WASD 廃止しカーソルキーのみにするのが動詞キーを英字で揃える前提。影響が広いので、タブ画面と既存動詞の
  配線を先に通し、移動キーの切替・投げるは段階的に足す。
- 食べると飲むは同じ操作にまとめ、効果で食べる飲む/使うを振り分ける。分類コンポーネントは足さない。
- 動詞タブと画面タブは同じタブ帯部品と `UseTabMenu` を共有し、1カラムで見た目と操作を揃える。

## 進捗

- [ ] 上部タブ帯を `styled` へ共通部品として抽出する。`hooks.UseTabMenu` と組み合わせ、1カラムで使える形にする
- [ ] 動詞タブ画面 `ItemActionState` の骨組み。共有タブ帯 + `UseTabMenu` + バックパックの `Accept` 絞り込み表示 + Enter 即実行。1カラム
- [ ] 動詞レジストリと `Accept` 述語。既存で賄える 使う・読む・置く・調べる・食べる飲む を先に通す。食べる飲むは効果で振り分ける
- [ ] 移動を WASD 廃止しカーソルキーのみにする。斜めは Shift + 矢印。メニュー操作も矢印へ寄せる
- [ ] ダンジョン・各メニューからのショートカット直達とタブ切替。置く `D`・調べる `X`、フィールド情報の移設、投げる `T` の Shift 区別
- [ ] 調べる。アイテム詳細のポップアップ表示、`Description` 流用
- [ ] 投げる。`Throwable` + `ThrowActivity` + ターゲット選択
- [ ] 画面タブメニュー `CharacterState`。装備・スキルのタブ。装備は `EquipMenu` の機能を1カラムのタブへ畳む
- [ ] 既存の2カラムメニューを1カラムへ寄せる。`StatusState` ほか
- [ ] VRT と回帰テスト。各タブの表示・空タブ・直達キー・タブ切替・1カラム
- [ ] 既存アクションウィンドウ導線の整理。移行を見てから縮小
