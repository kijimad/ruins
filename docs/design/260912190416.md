---
status: in-progress # draft|accepted|in-progress|done|superseded|dropped
tags: [worldgen, gamedesign]
auto: needs-decision
---

# 北進サバイバル。寒冷勾配・深部施設・キューブ修理でクリアする

## 背景

現状は worldstream が東西へ無限ストリーミング・南北を rows で有界とする帯で、進行軸は東。だが東進に意味づけが無く、世界に終点も勝利条件も無い。プレイヤーは「何を目指すのか」が分からず、ゲームとして完結しない。

ゲームの目的を次に定める。

> あなたは宇宙トラベラー。汎用個人船「キューブ」が故障し、極寒の惑星に墜落した。北へ進むほど寒さは厳しくなる。深部の極寒地帯には施設が残っており、そこで得たアイテムや設備でキューブを修理し、惑星から脱出＝クリアする。

進行軸を東から北へ回すことで、「北ほど寒い」という直感的な難易度勾配と、「北極に近い極寒地帯の施設を目指す」という目的が自然に説明できる。

## 要件

1. worldstream の無限ストリーミング軸を東から北へ物理反転する。北は画面の上すなわち -Y。南北が無限、東西が有界になる。
2. 北へ進んだ距離が増えるほど周囲気温が下がる緯度勾配を気温計算に加える。
3. 北へ進んだ距離が一定しきい値を超えた深部でのみ施設が出現し始める。
4. 施設で得たアイテムや設備でキューブの修理を進められる。
5. キューブの修理が完了したら脱出でき、クリアとなる。
6. オープニングの進路説明を「東」から「北」へ更新する。

要件1は一度「物理軸は東のまま保ち方向は提示だけで北と示す」低リスク案を採ったが、上キーで北へ進む
直感を優先して物理反転を実施した。ストリーミング核・座標・SeamlessBand・生成系の地物/道/散布・
マクロ地図・セーブ項目改名・VRT golden を北向きへ書き換えた。生成系は X-無限前提だったので、
地物配置 Placement のリージョン割りを Y へ回し、道の結線も Y リージョン間に直した。

## 設計

### 進行軸の物理反転

`internal/worldstream/band.go` の無限軸を北へ回す。`eastIndex`→`northIndex`、東端生成・西端破棄・
リベースを北端生成・南端破棄・+chunkH リベースへ置き換える。北は -Y なので東進コードの符号鏡像になる。
帯ローカル座標は画面と一致させ、localY=0 が北端、絶対 Y は南向きに増加、北へ進むほど負へ伸びる。

```go
type Band struct {
	chunkW, chunkH consts.Tile
	cols, rows     consts.Chunk // 有界=東西(cols)、無限=南北(rows を北へ流す)
	northIndex     consts.Chunk // 北進したチャンク数。北へ進むほど増える
}
// ShouldShiftNorth(localY) = localY < centerSlot*chunkH  すなわち中央行の北へ出たら発火
// ShiftNorth: 南端行破棄 → 全体+chunkH リベース → northIndex++ → 北端行(絶対チャンクY=-northIndex)生成
```

生成系は「X=無限・Y=有界」を前提にしていたので、`Placement` のリージョン割りを Y へ、有界カウントを
`rows`→`cols` へ回す。道の結線 `crossingRoads`・`buildRoadOverlay` も Y リージョン間へ直す。これで地物が
北進で湧き、道は南北に走る。マクロ地図 `MacroRange` に `OriginY` を足し縦帯として描く。絶対 Y は
`SeamlessBand.LocalToAbsY` = `localY - NorthIndex*chunkH`。

### 寒冷の緯度勾配

`query.AmbientTemperatureAt` へ奥行き距離ベースの項を足す。現状の式は ステージ基準 + 季節/時間 +
タイル遮蔽 + 焚き火熱 で、緯度勾配が無い。実装済み。

```go
// 奥地ほど寒い緯度勾配を世界温度に折り込む。屋内は shelteredWorldTemp で緩和され、
// 深部の施設が暖を取れる避難所になる。末尾で引くと屋内外が同じだけ寒くなり避難所にならない。
// worldTemp = 季節 + 時間帯 - latitudeCold(world, x)
const (
	latitudeColdPerChunk = 1  // 1チャンク奥へ進むごとに下がる℃
	latitudeColdMax      = 40 // 下げ幅の上限
)

// 起点は初期帯の中央列 cols/2。開始地点を穏やかに保ち、緯度勾配は「そこからさらに奥ほど寒い」加算分。
// 絶対軸 X で測るので帯シフトをまたいでも連続で、シフトの瞬間に気温が飛ばない。
func latitudeCold(world w.World, x consts.Tile) int
func latitudeColdForDepth(chunksDeep int) int // 単調増加し latitudeColdMax で頭打ち
```

### 深部での施設出現

overworld 生成に「北進距離がしきい値以深のチャンクにだけ施設種を出す」規則を足す。しきい値までは寒さが増すだけで施設は出ず、深部で救済/目的地としての施設が現れる。

```go
// 北進距離 northDist がしきい値以上のチャンクでのみ施設を配置対象にする。
const facilityNorthThreshold consts.Tile = 0 // TBD: 実プレイで調整

func facilityAllowedAt(northDist consts.Tile) bool { return northDist >= facilityNorthThreshold }
```

### キューブ修理

キューブの修理進捗を持つコンポーネントを新設する。施設で得たアイテムや設備で進捗を進める。

```go
// CubeRepair はキューブ修理の進捗。必要素材が揃い進捗が満ちると修理完了。
type CubeRepair struct {
	Progress int // 現在の修理進捗
	Required int // 完了に要する進捗
}
```

コンポーネント追加は add-component の手順（登録表 + make generate + serde 判断）に従う。

### クリア判定

修理完了で脱出可能になり、クリア状態へ遷移する。既存の撤退/クリア機構（docs の「クリア時ボーナス通貨」）と接続するかは要検討。

## 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/worldstream/{band,coord,worldstream,doc}.go` | 無限軸を東→北へ。`northIndex`、`ShiftNorth`、Y レンジ破棄、`BandOriginY`。実装済み |
| `internal/consts/coord.go` | `AbsTileX`→`AbsTileY`。北は -Y。実装済み |
| `internal/components/dungeon.go` | `SeamlessBand.EastIndex`→`NorthIndex`、`LocalToAbsY`。実装済み |
| `internal/overworld/{driver,chunkgen,feature,scatter,road*,macro_view,...}.go` | シフトを北へ。`Placement` を Y リージョンへ、有界カウント`cols`。道を南北へ。マクロ地図を縦帯へ。実装済み |
| `internal/world/query/{cube,temperature}.go` | 探索フォグと `NorthDepthChunks` を Y へ。単一出どころ。実装済み |
| `internal/systems/hud_extractor.go` + `internal/widgets/hud/`, `internal/states/overworld_map.go` | HUD `North N`、ミニマップ/全画面図を縦帯へ。実装済み |
| overworld 生成（施設しきい値） | 北へ進んだ距離しきい値以深でのみ施設を出す規則 |
| `internal/components`（新規 `CubeRepair`） | 修理進捗コンポーネント。登録表 + make generate |
| states（クリア） | 修理完了→脱出→クリアの遷移 |
| `internal/states/factories.go` + ja.po | オープニングの進路を東→北へ。実装済み |

## 採用しなかった方法

- **提示だけで北と示す低リスク案**（物理軸は東のまま、方位表示と物語だけで北と呼ぶ）。一度採ったが、上キーで北へ進む直感を優先して物理反転へ切り替えた。ゲーム内容は物理方向に依らないので提示案でも成立したが、操作の直感が損なわれる。
- **B. 有限の緯度世界**（南=温暖・開始 → 北=北極点=施設=脱出の有限マップ）。「北極点＝固定終点」を素直に実装できるが、無限ストリーミングの帯機構を丸ごと捨てる最大の改修になる。深部施設でAでもゴールが成立するため不採用。
- **C. 別案としての北ストリーミング＋しきい値ゴール**。本設計Aが「深部で施設が出現」を含むので、実質Aに統合された。

## コメント

- 未確定点。実プレイで詰める。
  - 寒冷勾配の急さ `coldPerTile` と気温の下限。
  - 施設出現の北進しきい値 `facilityNorthThreshold`。
  - 施設で得る修理素材/設備の具体と、`CubeRepair.Required` の量。
  - クリア後の扱い。既存のクリア時ボーナス通貨機構と接続するか。
- 実装順。緯度勾配・オープニング・HUD 表示・軸の物理反転まで完了。
- 軸反転は座標系の広範な追随を伴ったので、VRT golden と worldstream の既存テストで挙動を固定した。生成系の地物/道は Y リージョンへ回し、scatter golden の縦に走る道で正しさを目視確認した。

## 進捗

- [x] worldstream の無限軸を東→北へ物理反転する。生成系の地物/道/散布・マクロ地図まで含めて北向きへ
- [x] 奥行き距離ベースの寒冷勾配を気温計算に足す
- [x] オープニングの進路説明を東→北へ更新する
- [x] 方位表示で無限軸の奥行きを「北/奥地」と提示する。HUD 右下に `North N` を表示

施設出現・キューブ修理・修理完了でのクリアは軸反転と独立なので、本 doc の進捗から外した。
要件3-5と対応する設計節はその別タスクの下敷きとして残す。いつか別の doc/タスクで扱う。
