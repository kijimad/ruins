---
status: done # draft|accepted|in-progress|done|superseded|dropped
tags: [save, ui]
auto: needs-decision
---

# セーブ画面に保存時刻でなくプレイ実時間を表示する

## 背景

セーブ・ロード画面のスロットラベルは現在、保存時刻を `プレイヤー名  MM/DD HH:MM` の形で表示している。
保存時刻より、そのセーブでどれだけ遊んだかの累積プレイ実時間のほうが、続きを選ぶ判断に役立つ。

プレイ実時間は保存時刻から導けない累積量なので、計測して保存する機構が要る。ゲーム内時間 GameTime は
日数・季節を表す別物で、実時間ではない。

## 要件

- プレイ実時間はゲームを遊んでいた wall-clock の累積。ゲーム内経過ではない。
- ラン進行中だけ加算する。メインメニュー滞在は数えない。ラン中のサブメニューは数える。
- セーブに保存し、ロードで継続する。新規開始で0に戻る。
- スロットラベルに `時:分:秒` で表示する。時は無制限、分と秒は0埋め。例 `101:34:07`。
- 保存時刻 Timestamp は残す。ListSaves の最新ロード並べ替えに使うため。

## 設計

### PlayTime シングルトンコンポーネント

累積プレイ実時間を持つ。`GameTime` と同じくシングルトンに載せ、serde 保存対象にする。

```go
// PlayTime はそのランの累積プレイ実時間を保持するシングルトン。ゲーム内時間 GameTime とは別で、
// wall-clock で遊んだ時間を溜める。
type PlayTime struct {
	Duration time.Duration
}
```

- 登録表 `genspec/registry.go` に `{Field: "PlayTime"}` を足し `make generate`。
- `world.go` の `InitSingleton` で `world.Components.PlayTime.Add(singleton, &gc.PlayTime{})` を追加。
  `ResetForNewGame` は `InitSingleton` を呼ぶので、新規開始で0に戻る。
- `skipComponents` には入れない。ロードで継続させるため保存する。

### 加算 — セッション基準からセーブ時に1度畳む

PlayTime はセーブ画面のラベルでしか読まないので、毎フレーム積算する必要はない。セッションの計測基準
`PlayTimeSessionStart` を持ち、セーブのたびに経過を1度だけ畳む。毎フレーム積算の総和は telescoping で
「基準→セーブの経過」に等しく、結果は一致する。

基準は `Resources` に持つ。serde 非対象のセッション状態で、`InputSource` と同じく world 単位で持ち
グローバル可変を作らない。

```go
// ラン開始で基準を今に置く。ここより前のメインメニュー滞在は数えない
func (world World) ResetForNewGame() {
	// ... 実体クリア + InitSingleton ...
	world.Resources.PlayTimeSessionStart = time.Now()
}

// 再開も基準を今に置く。RestoreWorldFromJSON の末尾
world.Resources.PlayTimeSessionStart = time.Now()

// セーブ時に直列化の前で畳む。GenerateWorldJSON の冒頭
func accruePlayTime(world w.World) {
	start := world.Resources.PlayTimeSessionStart
	if start.IsZero() {
		return // 基準ゼロのラン外は数えない
	}
	pt := query.GetPlayTime(world)
	if pt == nil {
		return
	}
	now := time.Now()
	pt.Duration += now.Sub(start)
	world.Resources.PlayTimeSessionStart = now
}
```

`query.GetPlayTime(world) *gc.PlayTime` を singleton.go に足す。メインメニュー滞在は基準が未設定なので
自然に除外され、ラン中のサブメニューは基準セット後なので数えられる。

### 保存 — envelope へコピー

`saveEnvelope` に `PlayTime` を足し、保存時に singleton から書き出す。`PlayerName` と同じ2段構え
にし、ラベル表示で world 全体をロードせずに読めるようにする。

```go
type saveEnvelope struct {
	Version    string          `json:"version"`
	Timestamp  time.Time       `json:"timestamp"`
	PlayTime   time.Duration   `json:"playTime"`
	Checksum   string          `json:"checksum"`
	PlayerName string          `json:"playerName"`
	World      json.RawMessage `json:"world"`
}
```

- 保存時 `PlayTime: extractPlayTime(world)`。`extractPlayTime` は singleton の Duration を返す。
- `GetSavePlayTime(slot) (time.Duration, error)` を足し、`env.PlayTime` だけ部分 unmarshal する。

### 表示 — 時:分:秒

`formatSaveSlotLabel` を保存時刻でなくプレイ時間にする。

```go
d, ptErr := saveManager.GetSavePlayTime(slotName)
if nameErr == nil && ptErr == nil {
	h := int(d / time.Hour)
	m := int(d%time.Hour / time.Minute)
	s := int(d%time.Minute / time.Second)
	return fmt.Sprintf("%s  %d:%02d:%02d", playerName, h, m, s)
}
```

## 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/components/play_time.go` | 新規。`PlayTime` コンポーネント |
| `internal/components/genspec/registry.go` | `{Field: "PlayTime"}` を足す |
| `internal/world/world.go` | `InitSingleton` で `PlayTime` を初期化。`ResetForNewGame` で計測基準をセット |
| `internal/world/query/singleton.go` | `GetPlayTime` を足す |
| `internal/resources/resources.go` | `PlayTimeSessionStart` セッション基準を持つ |
| `internal/save/serde.go` | `saveEnvelope.PlayTime`・`extractPlayTime`・`accruePlayTime` |
| `internal/save/manager.go` | 保存時に `accruePlayTime` で畳み `PlayTime` を書く。`RestoreWorldFromJSON` で再開基準をセット。`GetSavePlayTime` |
| `internal/states/factories.go` | `formatSaveSlotLabel` を時:分:秒表示に替える |

## なぜこの形か

- **PlayerName の2段構えを踏襲する**: 累積は world の singleton に持ち、表示用に envelope へコピーする。
  ラベル表示で world 全体をロードしないという既存の効率を保つ。
- **Timestamp を残す**: `ListSaves` の最新ロード並べ替えは Timestamp 降順に依存する。表示をプレイ時間に
  替えても、同定・並べ替えのキーは時刻のまま分離する。
- **セーブ時に1度だけ畳む**: PlayTime はセーブ画面でしか読まないので、毎フレーム積算は不要。基準からの
  経過をセーブ時に1度足せば同じ結果になる。計測基準を run 開始と再開でセットするので、メインメニュー滞在は
  基準未設定で数えず、ラン中のサブメニューは基準セット後なので数える。プレイヤー存在の毎フレーム判定は要らない。

## 採用しなかった方法

- **ゲーム内時間 GameTime を表示する**: 実時間でなく日数・季節。要件と異なる。GameTime とは別に持つ。
- **累積 PlayTime を Resources に持つ**: Resources は serde 非対象なので保存されない。継続に singleton
  コンポーネントが要る。累積は singleton、計測基準だけをセッション状態として Resources に持つ。
- **保存時刻 Timestamp を廃してプレイ時間で並べ替える**: プレイ時間は「最後にセーブした順」を表さない。
  最新ロードの意味が壊れる。Timestamp を並べ替えに残す。
- **毎フレーム `time.Since` を積算する**: 総和は telescoping で基準→セーブの経過に等しく結果は同じだが、
  読むのはセーブ時だけなので毎フレームの計測は無駄。基準からセーブ時に1度畳む。
- **フレーム数 × 1/TPS で近似する**: フレーム落ち時に wall-clock からずれる。`time.Since` の実測にする。

## 検証

- `make check`。
- ユニット: `PlayTime` の serde 往復、`GetSavePlayTime` の部分読み、`formatSaveSlotLabel` の時:分:秒整形、
  `accruePlayTime` がセーブ時にセッション経過を足す・基準ゼロのラン外では足さない。
- 目視: 遊んで保存し、ロード画面に時:分:秒が出て、続けると増えることを確認する。

## リスク・影響範囲

- 未リリースなのでセーブ後方互換は考慮不要。旧セーブに PlayTime が無くてもゼロ値に落ちるだけ。
- セーブ時に `time.Now` を1度呼ぶだけで、毎フレームの計測は無い。

## 進捗

- [x] `PlayTime` コンポーネントを足し `make generate`
- [x] `InitSingleton` と `GetPlayTime` を足す
- [x] セッション基準 `PlayTimeSessionStart` と `accruePlayTime` でセーブ時に畳む
- [x] `saveEnvelope`・`extractPlayTime`・保存時書き込み・`GetSavePlayTime`
- [x] `formatSaveSlotLabel` を時:分:秒表示に替える
- [x] ユニットテストを書く
- [x] `make check` を通す
