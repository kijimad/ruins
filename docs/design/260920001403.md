---
status: done # draft|accepted|in-progress|done|superseded|dropped
tags: [save, ui]
auto: needs-decision
---

# セーブ画面に保存時刻でなくプレイ実時間を表示する

## 背景

セーブ・ロード画面のスロットラベルは現在、保存時刻を `プレイヤー名  MM/DD HH:MM` の形で表示している。
保存時刻より、そのセーブでどれだけ遊んだかの累積プレイ実時間のほうが、続きを選ぶ判断に役立つ。

プレイ実時間は保存時刻から導けない累積量なので、計測して保存する機構が要る。ゲーム内時間 GameTime は
日数・季節を表す別物で、実時間ではない。

## 要件

- プレイ実時間はゲームを遊んでいた wall-clock の累積。ゲーム内経過ではない。
- ラン進行中だけ加算する。メインメニュー滞在は数えない。ラン中のサブメニューは数える。
- セーブに保存し、ロードで継続する。新規開始で0に戻る。
- スロットラベルに `時:分:秒` で表示する。時は無制限、分と秒は0埋め。例 `101:34:07`。
- 保存時刻 Timestamp は残す。ListSaves の最新ロード並べ替えに使うため。

## 設計

### 保存は envelope、現在値は skipComponents のシングルトン

累積プレイ実時間の永続の実体は `saveEnvelope.PlayTime`。envelope はラベルの高速部分読みのために既に
PlayTime を持つので、これを唯一の保存先にする。world スナップショットへは二重保存しない。

セッション中の現在値は `PlayTime` シングルトンコンポーネントに持ち、`skipComponents` に入れて直列化しない。
`VisionState`・`SpatialIndex` と同じ「world 単位・serde 非対象のセッション状態」の置き場に揃える。

```go
// serde 非対象。永続は envelope。SessionStart のゼロ値はラン外
type PlayTime struct {
	Total        time.Duration
	SessionStart time.Time
}
```

- 登録表に `{Field: "PlayTime"}` を足し `make generate`。`InitSingleton` で付与する。
- `skipComponents` に入れる。ロードでは `reestablishSingleton` が付け直し、`RestoreWorldFromJSON` が
  `Total = env.PlayTime` と envelope から seed する。`VisionState` の再構築と同じ流儀。
- 新規開始 `ResetForNewGame` で `SessionStart` を今にセット。Total は InitSingleton で 0。

### 加算 — セッション基準からセーブ時に1度畳む

PlayTime はセーブ画面のラベルでしか読まないので、毎フレーム積算する必要はない。`SessionStart` を計測基準に、
セーブのたびに経過を1度だけ `Total` へ畳む。毎フレーム積算の総和は telescoping で「基準→セーブの経過」に
等しく、結果は一致する。

```go
// セーブ時に畳む。GenerateWorldJSON で envelope へ書く前に呼ぶ
func accruePlayTime(world w.World) {
	pt := query.GetPlayTime(world)
	if pt == nil || pt.SessionStart.IsZero() {
		return // 基準ゼロのラン外は数えない
	}
	now := time.Now()
	pt.Total += now.Sub(pt.SessionStart)
	pt.SessionStart = now
}
```

メインメニュー滞在は基準が未設定なので自然に除外され、ラン中のサブメニューは基準セット後なので数えられる。

### 保存 — envelope が唯一の保存先

`saveEnvelope` に `PlayTime` を足し、保存時に `accruePlayTime` で畳んだ現在値を書き出す。`PlayerName` と
同じくラベル表示で world 全体をロードせずに読める。累積の永続はこの envelope だけが担い、world スナップショットへは
重複させない。

```go
type saveEnvelope struct {
	Version    string          `json:"version"`
	Timestamp  time.Time       `json:"timestamp"`
	PlayTime   time.Duration   `json:"playTime"`
	Checksum   string          `json:"checksum"`
	PlayerName string          `json:"playerName"`
	World      json.RawMessage `json:"world"`
}
```

- 保存時 `PlayTime: extractPlayTime(world)`。`extractPlayTime` は `PlayTime.Total` を返す。
- `GetSavePlayTime(slot) (time.Duration, error)` を足し、`env.PlayTime` だけ部分 unmarshal する。

### 表示 — 時:分:秒

`formatSaveSlotLabel` を保存時刻でなくプレイ時間にする。

```go
d, ptErr := saveManager.GetSavePlayTime(slotName)
if nameErr == nil && ptErr == nil {
	h := int(d / time.Hour)
	m := int(d%time.Hour / time.Minute)
	s := int(d%time.Minute / time.Second)
	return fmt.Sprintf("%s  %d:%02d:%02d", playerName, h, m, s)
}
```

## 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/components/play_time.go` | 新規。`PlayTime{Total, SessionStart}` シングルトン |
| `internal/components/genspec/registry.go` | `{Field: "PlayTime"}` を足す |
| `internal/world/world.go` | `InitSingleton` で付与。`ResetForNewGame` で計測基準をセット |
| `internal/world/query/singleton.go` | `GetPlayTime` を足す |
| `internal/save/serde.go` | `saveEnvelope.PlayTime`・`extractPlayTime`・`accruePlayTime`・`skipComponents` へ追加・`reestablishSingleton` で付け直し |
| `internal/save/manager.go` | 保存時に `accruePlayTime` で畳み `PlayTime` を envelope へ書く。`RestoreWorldFromJSON` で envelope から seed し再開基準をセット。`GetSavePlayTime` |
| `internal/states/factories.go` | `formatSaveSlotLabel` を時:分:秒表示に替える |

## なぜこの形か

- **保存先を envelope に一本化する**: envelope はラベルの高速部分読みのため PlayTime を既に持つ。`skipComponents`
  に入れて world スナップショットへは直列化せず、二重保存を避ける。ロードで envelope から seed する。
- **セッション状態は skipComponents のシングルトンに置く**: `VisionState`・`SpatialIndex` と同じく、world 単位で
  serde 非対象のセッション状態はシングルトンコンポーネントに持つのが既存の流儀。Resources はアセット・config 置き場で
  収まりが悪い。
- **Timestamp を残す**: `ListSaves` の最新ロード並べ替えは Timestamp 降順に依存する。表示をプレイ時間に
  替えても、同定・並べ替えのキーは時刻のまま分離する。
- **セーブ時に1度だけ畳む**: PlayTime はセーブ画面でしか読まないので、毎フレーム積算は不要。基準からの
  経過をセーブ時に1度足せば同じ結果になる。計測基準を run 開始と再開でセットするので、メインメニュー滞在は
  基準未設定で数えず、ラン中のサブメニューは基準セット後なので数える。プレイヤー存在の毎フレーム判定は要らない。

## 採用しなかった方法

- **ゲーム内時間 GameTime を表示する**: 実時間でなく日数・季節。要件と異なる。GameTime とは別に持つ。
- **PlayTime を serde 対象コンポーネントにする**: world スナップショットへ保存されるが envelope も同じ値を持つので
  二重保存になる。`skipComponents` に入れて envelope から seed すれば重複しない。
- **累積とセッション基準を Resources に持つ**: Resources はアセット・config 置き場で、セッション進行状態の収まりが
  悪い。既存の serde 非対象セッション状態は skipComponents のシングルトンに揃える。
- **保存時刻 Timestamp を廃してプレイ時間で並べ替える**: プレイ時間は「最後にセーブした順」を表さない。
  最新ロードの意味が壊れる。Timestamp を並べ替えに残す。
- **毎フレーム `time.Since` を積算する**: 総和は telescoping で基準→セーブの経過に等しく結果は同じだが、
  読むのはセーブ時だけなので毎フレームの計測は無駄。基準からセーブ時に1度畳む。
- **フレーム数 × 1/TPS で近似する**: フレーム落ち時に wall-clock からずれる。`time.Since` の実測にする。

## 検証

- `make check`。
- ユニット: `PlayTime` の serde 往復、`GetSavePlayTime` の部分読み、`formatSaveSlotLabel` の時:分:秒整形、
  `accruePlayTime` がセーブ時にセッション経過を足す・基準ゼロのラン外では足さない。
- 目視: 遊んで保存し、ロード画面に時:分:秒が出て、続けると増えることを確認する。

## リスク・影響範囲

- 未リリースなのでセーブ後方互換は考慮不要。旧セーブに PlayTime が無くてもゼロ値に落ちるだけ。
- セーブ時に `time.Now` を1度呼ぶだけで、毎フレームの計測は無い。

## 進捗

- [x] `PlayTime{Total, SessionStart}` コンポーネントを足し `make generate`。`skipComponents` へ追加
- [x] `InitSingleton`・`GetPlayTime`・`reestablishSingleton` で付与、`RestoreWorldFromJSON` で envelope から seed
- [x] `accruePlayTime` でセーブ時に畳む
- [x] `saveEnvelope`・`extractPlayTime`・保存時書き込み・`GetSavePlayTime`
- [x] `formatSaveSlotLabel` を時:分:秒表示に替える
- [x] ユニットテストを書く
- [x] `make check` を通す
