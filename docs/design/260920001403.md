---
status: done # draft|accepted|in-progress|done|superseded|dropped
tags: [save, ui]
auto: needs-decision
---

# セーブ画面に保存時刻でなくプレイ実時間を表示する

## 背景

セーブ・ロード画面のスロットラベルは現在、保存時刻を `プレイヤー名  MM/DD HH:MM` の形で表示している。
保存時刻より、そのセーブでどれだけ遊んだかの累積プレイ実時間のほうが、続きを選ぶ判断に役立つ。

プレイ実時間は保存時刻から導けない累積量なので、計測して保存する機構が要る。ゲーム内時間 GameTime は
日数・季節を表す別物で、実時間ではない。

## 要件

- プレイ実時間はゲームを遊んでいた wall-clock の累積。ゲーム内経過ではない。
- ラン進行中だけ加算する。メインメニュー滞在は数えない。ラン中のサブメニューは数える。
- セーブに保存し、ロードで継続する。新規開始で0に戻る。
- スロットラベルに `時:分:秒` で表示する。時は無制限、分と秒は0埋め。例 `101:34:07`。
- 保存時刻 Timestamp は残す。ListSaves の最新ロード並べ替えに使うため。

## 設計

### 保存は envelope、現在値は skipComponents のシングルトン

累積プレイ実時間の永続の実体は `saveEnvelope.PlayTime`。envelope はラベルの高速部分読みのために既に
PlayTime を持つので、これを唯一の保存先にする。world スナップショットへは二重保存しない。

セッション中の現在値は `PlayTime` シングルトンコンポーネントに持ち、`skipComponents` に入れて直列化しない。
`VisionState`・`SpatialIndex` と同じ「world 単位・serde 非対象のセッション状態」の置き場に揃える。

単一の累積 `total` を毎フレーム進める、他ゲームと同じストップウォッチ方式にする。フィールドは非公開にし、
`Start`・`Tick`・`Elapsed` の3メソッドで用法を閉じる。`total` は累積で常に最新、`lastTick` は前回計測時刻。

```go
type PlayTime struct {
	total    time.Duration
	lastTick time.Time
}

// Start は prior を累積の初期値にし、起点を now に置く。now を注入し純粋にすることでテストが決定的になる
func (pt *PlayTime) Start(prior time.Duration, now time.Time) {
	pt.total = prior
	pt.lastTick = now
}

// Tick はラン進行中に毎フレーム呼び、前回からの実経過を累積へ足す。初回は起点だけ置く
func (pt *PlayTime) Tick(now time.Time) {
	if !pt.lastTick.IsZero() {
		pt.total += now.Sub(pt.lastTick)
	}
	pt.lastTick = now
}

// Elapsed は現在の累積を返す。Tick で常に最新なので total をそのまま返す。nil セーフ
func (pt *PlayTime) Elapsed() time.Duration {
	if pt == nil {
		return 0
	}
	return pt.total
}
```

- 登録表に `{Field: "PlayTime"}` を足し `make generate`。`InitSingleton` で付与する。
- `skipComponents` に入れる。ロードでは `reestablishSingleton` が付け直しと同時に envelope の値で
  `pt.Start(env.PlayTime, now)` まで済ませる。付与と seed を1関数に閉じ、順序依存を作らない。
- 新規開始 `ResetForNewGame` で `pt.Start(0, now)`。

### 測る — 毎フレーム累積

パフォーマンスは問題にならないので、他ゲームと同じく累積を毎フレーム進める。`MainGame.Update` がラン進行中
(プレイヤー在)だけ `Tick` を呼び、前回からの実経過を足す。`total` が常に最新の答えなので、保存も統計表示も
`Elapsed()` で読むだけ。

メインメニュー滞在は `Tick` を呼ばないので数えず、ラン中のサブメニューはプレイヤーが居るので数える。
`time.Now` の単調時計が効くので、セッション中の壁時計調整にも影響されない。

### 保存 — envelope が唯一の保存先

`saveEnvelope` に `PlayTime` を足し、保存時に `GetPlayTime(world).Elapsed()` の値を書き出す。`PlayerName` と
同じくラベル表示で world 全体をロードせずに読める。累積の永続はこの envelope だけが担い、world スナップショットへは
重複させない。

### 統計画面にも出す

`RunStatsState` の Summary タブに「Play time」行を足し、`GetPlayTime(world).Elapsed()` を `formatPlayTime` で
時:分:秒に整形して見せる。値は live で、セーブ画面ラベルと同じ導出を共有する。

```go
type saveEnvelope struct {
	Version    string          `json:"version"`
	Timestamp  time.Time       `json:"timestamp"`
	PlayTime   time.Duration   `json:"playTime"`
	Checksum   string          `json:"checksum"`
	PlayerName string          `json:"playerName"`
	World      json.RawMessage `json:"world"`
}
```

- 保存時 `PlayTime: query.GetPlayTime(world).Elapsed()`。
- `GetSavePlayTime(slot) (time.Duration, error)` を足し、`env.PlayTime` だけ部分 unmarshal する。

### 表示 — 時:分:秒を右寄せ

`formatSaveSlotLabel` は左ラベル(プレイヤー名)と右寄せのプレイ時間を返す。`formatPlayTime` が時:分:秒へ
整形する。時は無制限、分と秒は0埋め。

```go
func formatPlayTime(d time.Duration) string {
	h := int(d / time.Hour)
	m := int(d % time.Hour / time.Minute)
	s := int(d % time.Minute / time.Second)
	return fmt.Sprintf("%d:%02d:%02d", h, m, s)
}
```

`Choice.Value` に時間を渡すと、`ChoiceMenuState.ViewUI` が `[Name(左), Num(右寄せ)]` の2列で描き、時間が
右端へ揃う。1文字列を空白で押し出すのは比例フォントで崩れるため、列の整列で表す。`Value` を使わない他メニューは
1列のまま。

## 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/components/play_time.go` | 新規。`PlayTime`＋`Start`/`Tick`/`Elapsed` メソッド |
| `internal/components/genspec/registry.go` | `{Field: "PlayTime"}` を足す |
| `internal/world/world.go` | `InitSingleton` で付与。`ResetForNewGame` で `Start(0, now)` |
| `internal/maingame/game.go` | ラン進行中だけ `Update` で `Tick` を呼ぶ |
| `internal/world/query/singleton.go` | `GetPlayTime` |
| `internal/save/serde.go` | `saveEnvelope.PlayTime`・`skipComponents` へ追加・`reestablishSingleton` で付け直し＋envelope 値で `Start` |
| `internal/save/manager.go` | 保存時に `Elapsed()` を envelope へ書く。`restoreInto` に seed 値を通す。`GetSavePlayTime` |
| `internal/states/choice.go` | `Choice.Value` を足し、使用時のみ右寄せ列で描く |
| `internal/states/factories.go` | `formatSaveSlotLabel` を時:分:秒に。プレイ時間を右寄せで並べる |
| `internal/states/run_stats_state.go` | Summary タブに Play time 行を足す |
| `internal/i18n/locale/ja.po` | `Play time` の訳を足す |

## なぜこの形か

- **保存先を envelope に一本化する**: envelope はラベルの高速部分読みのため PlayTime を既に持つ。`skipComponents`
  に入れて world スナップショットへは直列化せず、二重保存を避ける。ロードで envelope から seed する。
- **セッション状態は skipComponents のシングルトンに置く**: `VisionState`・`SpatialIndex` と同じく、world 単位で
  serde 非対象のセッション状態はシングルトンコンポーネントに持つのが既存の流儀。Resources はアセット・config 置き場で
  収まりが悪い。
- **Timestamp を残す**: `ListSaves` の最新ロード並べ替えは Timestamp 降順に依存する。表示をプレイ時間に
  替えても、同定・並べ替えのキーは時刻のまま分離する。
- **毎フレーム累積で測る**: パフォーマンスが問題にならないので、他ゲームと同じく累積を毎フレーム進める。`total` が
  常に最新の答えなので、保存も統計も `Elapsed()` で読むだけ。`Start`・`Tick`・`Elapsed` の3メソッドに用法を閉じ、
  フィールド操作を散らさない。`Tick` を `MainGame.Update` でプレイヤー在のときだけ呼ぶので、メインメニュー滞在は数えず、
  ラン中のサブメニューは数える。`time.Now` の単調時計が効き、壁時計調整にも強い。

## 採用しなかった方法

- **ゲーム内時間 GameTime を表示する**: 実時間でなく日数・季節。要件と異なる。GameTime とは別に持つ。
- **PlayTime を serde 対象コンポーネントにする**: world スナップショットへ保存されるが envelope も同じ値を持つので
  二重保存になる。`skipComponents` に入れて envelope から復元すれば重複しない。
- **累積とセッション基準を Resources に持つ**: Resources はアセット・config 置き場で、セッション進行状態の収まりが
  悪い。既存の serde 非対象セッション状態は skipComponents のシングルトンに揃える。
- **開始時刻を保存し読むとき遅延計算する**: 蓄積＋開始時刻の2フィールドや単一の仮想開始時刻(`now - 蓄積`)で、
  読むときに `time.Since` する形。毎フレーム書き込みを避ける最適化だが、本作はパフォーマンスが問題にならず、累積を
  毎フレーム進める素直な形のほうが「値そのものを読むだけ」で分かりやすい。当初この遅延形にしていたが、標準の毎フレーム
  累積へ戻した。
- **保存時刻 Timestamp を廃してプレイ時間で並べ替える**: プレイ時間は「最後にセーブした順」を表さない。
  最新ロードの意味が壊れる。Timestamp を並べ替えに残す。
- **フレーム数 × 1/TPS で近似する**: フレーム落ち時に wall-clock からずれる。`time.Now` の実経過を足す。

## 検証

- `make check`。
- ユニット: 基準からの経過を保存しロードで継続する、`GetSavePlayTime` の部分読み、基準ゼロのラン外は0、
  `formatPlayTime` の時:分:秒整形。
- 目視: 遊んで保存し、ロード画面に時:分:秒が右寄せで出て、続けると増えることを確認する。

## リスク・影響範囲

- 未リリースなのでセーブ後方互換は考慮不要。旧セーブに PlayTime が無くてもゼロ値に落ちるだけ。
- 毎フレーム `time.Now` を1回呼び累積へ足す。ターン制なので負荷は無視できる。

## 進捗

- [x] `PlayTime`＋`Start`/`Tick`/`Elapsed` メソッドを足し `make generate`。`skipComponents` へ追加
- [x] `InitSingleton`・`GetPlayTime`・`reestablishSingleton` で付与＋envelope 値で `Start`（順序依存を作らない）
- [x] `MainGame.Update` で `Tick`。保存は `Elapsed()`。`GetSavePlayTime`
- [x] `formatSaveSlotLabel` を時:分:秒に。プレイ時間を右寄せで並べる
- [x] 統計画面の Summary タブに Play time 行を足す
- [x] ユニットテストを書く
- [x] `make check` を通す
