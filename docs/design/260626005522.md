---
status: done
tags: []
auto: needs-decision
---

# 小部屋マップの入口にランダムにドアを生成する

## 背景

小部屋タイプのマップビルダー（`NewSmallRoomPlanner`）で生成されるダンジョンには、部屋と廊下が存在するが、部屋の入口にドアがない。ローグライクとして、閉じた扉が部屋の入口にあることで探索の緊張感や戦術性が生まれる。

### 要件

- 部屋の入口にランダムな確率でドアを配置する
- 3タイル幅の廊下が部屋との接続部で1タイル幅に狭まるようにする
- 既存の扉システム（開閉、ロック、BlockPass/BlockView）をそのまま活用する

## 設計

### 方針: タイルパターン検出によるドア配置

digger_rsの方式を参考に、全タイルを走査して「床タイルの左右が壁かつ上下が床」または「上下が壁かつ左右が床」のパターンを検出し、ドアを配置する。部屋境界の走査やグループ化は不要で、タイルパターンのみで判定する。

廊下が部屋との接続部で1タイル幅に狭まるのは `LineCorridorPlanner` が担当する。部屋に隣接するサイドタイルを床に変換しないことで、自然に1タイル幅の入口が生まれる。

### PlannerChain内の位置

```
RectRoomPlanner        // 部屋を計画
FillAll("wall")        // 全体を壁で埋める
RoomDraw               // 部屋を描画
LineCorridorPlanner    // 廊下を作成（部屋境界で1タイル幅に狭まる）
DoorPlanner            // 入口にランダムにドアを配置  ← 新規
ConvertIsolatedWalls   // 床に隣接しない壁をvoidに変換
EnvironmentPlanner     // 環境情報を計算
```

### データ構造

```go
// DoorSpec はドア配置仕様を表す
type DoorSpec struct {
    consts.Coord[int]
    Orientation gc.DoorOrientation // 計画フェーズで確定済み
}
```

### スポーン

`mapspawner/spawner.go` の `spawnDoors` で `DoorSpec.Orientation` をそのまま使用する。向きは計画フェーズで確定済みのため、スポーン時の再計算は不要。

### 変更箇所

| ファイル | 変更内容 |
|---------|---------|
| `mapplanner/lib.go` | `DoorSpec`構造体追加、`MetaPlan.Doors`フィールド追加、`existPlannedEntityOnTile`にドアチェック追加、`NewSmallRoomPlanner`チェーンにDoorPlanner追加 |
| `mapplanner/door_planner.go` | 新規。タイルパターン検出によるドア配置 |
| `mapplanner/door_planner_test.go` | 新規。パターン検出とDoorPlanner統合テスト |
| `mapplanner/line_corridor.go` | 部屋境界での廊下狭め処理を追加 |
| `mapplanner/line_corridor_test.go` | 新規。廊下狭めテストとisAdjacentToRoomテスト |
| `mapspawner/spawner.go` | `spawnDoors`関数追加 |

### 採用しなかった方法

| 方法 | 不採用の理由 |
|------|-------------|
| 部屋境界走査→グループ化→狭め方式 | 4辺走査、グループ化、壁戻しなどロジックが複雑。タイルパターン検出で十分 |
| LineCorridorPlanner内で入口座標を記録する | 廊下生成ロジックと入口検出ロジックが混在して責務が曖昧になる |
| 廊下を全体的に1タイル幅にする | 見た目の開放感が失われる。部屋境界のみ狭めれば十分 |

## コメント

- `DoorChance: 0.8` でドア配置確率を80%に設定している。ゲームバランスに応じて調整可能
- DoorPlannerのRNG消費により、固定シードを使うテスト（`TestHostileNPCPlanner_PlanMeta`）のシードを99999→100に変更した
- `DoorSpec` に `Orientation` を持たせることで、計画フェーズとスポーンフェーズの二重計算を排除している

## 進捗

- [x] 部屋の入口にランダム確率でドアを配置する。`mapplanner/door_planner.go` の `DoorPlanner.PlanMeta` が全タイルを走査し、`mp.RNG.Float64() >= p.DoorChance` で確率判定する
- [x] `DoorChance: 0.8` でチェーンに組み込む。`lib.go:368` の `NewSmallRoomPlanner` で `chain.With(DoorPlanner{DoorChance: 0.8})` を廊下作成の直後、`ConvertIsolatedWalls` の直前に配置
- [x] タイルパターン検出でドア位置を判定する。`door_planner.go` の `isDoorPattern` が左右壁かつ上下床、または上下壁かつ左右床を検出する
- [x] 3タイル幅の廊下が接続部で1タイル幅に狭まる。`line_corridor.go:79-83` でサイドタイル描画時に `isAdjacentToRoom` が真ならスキップする。`isAdjacentToRoom` は部屋矩形を1タイル膨張させて判定する
- [x] `DoorSpec` 構造体を追加する。`lib.go:26-30` に `consts.Coord[consts.Tile]` 埋め込みと `Orientation gc.DoorOrientation` を持つ。設計本文の `consts.Coord[int]` 表記に対し実装は `consts.Coord[consts.Tile]` で型付けしている
- [x] `MetaPlan.Doors` フィールドを追加する。`lib.go:55-56` に `Doors []DoorSpec`
- [x] `existPlannedEntityOnTile` にドアチェックを追加する。`lib.go:108-113` で `bm.Doors` を走査する
- [x] 向きを計画フェーズで確定する。`door_planner.go` の `doorOrientation` が `DoorSpec.Orientation` を埋め、スポーン時は再計算しない
- [x] `mapspawner/spawner.go` に `spawnDoors` を追加する。`spawner.go:207-231` で `metaPlan.Doors` を走査し `door.Orientation` をそのまま `lifecycle.SpawnDoor` に渡す
- [x] 既存の扉システムを活用する。`spawnDoors` が `lifecycle.SpawnDoor` を呼び、開閉・ロック・BlockPass/BlockView を持つ既存扉を生成する
- [x] `door_planner_test.go` を新設する。`TestDoorPlanner_PlanMeta`、`TestIsDoorPattern`
- [x] `line_corridor_test.go` を新設する。`TestLineCorridorPlanner_NarrowAtRoomBoundary`、`TestIsAdjacentToRoom`
- [x] 固定シードテストのシードを変更する。`hostile_npc_planner_test.go:212` の `NewSmallRoomPlanner(40, 40, 100)` で 99999 から 100 へ変更済み
