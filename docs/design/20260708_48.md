---
status: done
tags: []
auto: needs-decision
---

# 解像度は論理固定・ウィンドウは可変とする

## 背景

設計ドキュメント `20260708_47.md` でユーザー設定を永続化する器（`config.UserConfig` の `WindowWidth`/`WindowHeight` と `settings.toml`）を用意した。当初はこれを使って「解像度プリセットを選ぶ設定UI」を追加する方針で検討・実装した（本ドキュメントの初版）。

しかし実機で確認した結果、この方針は本ゲームには合わないと判明した。

- スプライトは **32x32 固定のピクセルアート**である。論理解像度を上げても1タイルあたりの情報量は増えず、単に**見える範囲（視野）が広がるだけ**になる。視野が解像度で変わるのはゲームバランス上望ましくない。
- 一方、論理解像度を固定してウィンドウだけ拡大縮小する方式では、960x720 の描画をスケールするだけで**見た目が変わらない**ため、解像度を選ぶUIに意味がない。

結論として、**論理解像度は 960x720 に固定し、ウィンドウサイズだけを可変にする**のが本ゲームに適する。ウィンドウの可変化は Ebiten の `WindowResizingModeEnabled`（`cmd/play.go` で設定済み）で既に実現されているため、解像度を切り替えるUIは不要である。

本ドキュメントは、検討の結果として解像度UIを設けない判断と、それに伴う最小限の修正を記録する。

## 要件

- 論理解像度は 960x720 固定とする（ピクセルアートのため）
- ウィンドウは可変とする（ユーザーがドラッグでリサイズできる。Ebiten が固定解像度をスケールする）
- 解像度を切り替える設定UIは設けない（意味がないため）
- 描画の基準（カメラ中心・HUD配置）が、固定論理解像度と一致していること
- グローバル設定画面（メインメニューからの「設定」）は残す。将来の設定（音量など）を追加する土台とする

## 設計

### 論理解像度は固定、ウィンドウは Ebiten のリサイズに任せる

`MainGame.Layout()` は従来どおり `consts.GameWidth/Height`（960x720）を返す。ウィンドウのリサイズは `cmd/play.go` の `ebiten.SetWindowResizingMode(ebiten.WindowResizingModeEnabled)` で有効化済みで、Ebiten が 960x720 の描画をウィンドウサイズへ自動スケールする。追加の実装は不要である。

`config.UserConfig.WindowWidth/Height` は、起動時の**初期ウィンドウサイズ**として `cmd/play.go` の `ebiten.SetWindowSize` に渡す用途で残す（`settings.toml` や環境変数で初期サイズを指定できる）。論理解像度とは別概念である。

### ScreenDimensions を論理解像度に固定する（潜在バグ修正）

`ScreenDimensions` はカメラ中心（`render_sprite.go` の `ScreenDimensions/2`）や HUD 配置（`widgets/hud/*`）の基準として参照される。これは**描画対象（`Layout()` が返す論理解像度 = 960x720）と一致していなければならない**。

従来は init で `cfg.User.WindowWidth/Height`（初期ウィンドウサイズ）をセットしていた。既定の 960x720 では偶然一致しているが、`settings.toml` や環境変数で初期ウィンドウサイズを 1280 などにすると、カメラ中心・HUD 位置が実際の描画面（960x720）とズレる潜在バグがある。

論理解像度は固定なので、`SetScreenDimensions` には**論理解像度（`consts.GameWidth/Height`）を渡す**。これによりウィンドウサイズと描画基準を分離し、初期ウィンドウサイズを変えても描画がズレないようにする。

```go
// 変更前
world.Resources.SetScreenDimensions(cfg.User.WindowWidth, cfg.User.WindowHeight)

// 変更後: 論理解像度に固定する
world.Resources.SetScreenDimensions(consts.GameWidth, consts.GameHeight)
```

### グローバル設定画面と言語設定（仮）

メインメニューからの「設定」（`SettingsMenuState`）を追加する。ただし解像度項目は設けない。状態管理は `hooks.Mount` + `UseTabMenu`、項目リストの描画は再利用可能な `tabmenu.View` に任せる（自前で項目ウィジェットを組まない）。値は `tabmenu.Item.AdditionalLabels` で項目の右側に表示する。

`hooks.UseTabMenu` は上下でアイテム移動、Tab でタブ切替、左右はページ送り（`ItemsPerPage > 0` のときのみ）を扱う。値の変更は左右キーではなく Enter モーダルで行うため、設定画面の入力は上下・Enter・キャンセルのみで済む。

設定項目として**言語設定を仮で追加**する。設定画面では現在値（例: 日本語）を項目の右側に表示し、**Enter でモーダル（言語選択の選択肢）を開いて**選ぶ。選択すると `config.UserConfig.Language` に保存して設定画面へ戻る。ただし**実際の表示言語の切り替え（i18n）は未実装**で、現状は設定値の保持のみを行う。将来 i18n を実装した際に、この保存値を参照する。

モーダルは既存の `MessageState` + `messagedata.WithChoice`（LoadMenu などと同じ宣言的な選択肢）で作る（`NewLanguageMenuState`）。左右キーでの値循環は採用しない（下記「採用しなかった方法」参照）。

- `config.UserConfig` に `Language string`（デフォルト `"ja"`）を追加する。永続化・環境変数上書き・デフォルト補完は `20260708_47.md` の仕組みにそのまま乗る
- 音量など将来の設定も、同様に `SettingsMenuState` の項目として追加し、値の選択はモーダルで行う

## 変更箇所

| ファイル | 変更内容 |
|---------|---------|
| `internal/maingame/game.go` | `SetScreenDimensions` に論理解像度（`consts.GameWidth/Height`）を渡すよう変更（潜在バグ修正）。`Layout()` は固定のまま、意図をコメントで明記 |
| `internal/config/config.go` | `UserConfig` に `Language`（デフォルト `"ja"`）を追加。説明はフィールドの行コメントに書く（`doc.go` の環境変数一覧は二重管理を避けて廃止済み） |
| `internal/states/settings_menu.go` | `SettingsMenuState`（設定画面）。項目描画は `tabmenu.View` に委譲。言語プリセット、`currentLanguageLabel`、`NewLanguageMenuState`（言語選択モーダル）。解像度ロジックは含めない（新規） |
| `internal/states/settings_menu_test.go` | `NewLanguageMenuState`・`currentLanguageLabel`・`fetchProps`・`DoAction`（選択・戻る・キャンセル）のテスト（新規） |
| `internal/states/golden_test.go` | `TestGolden_SettingsMenu`・`TestGolden_LanguageMenu` 追加 |
| `internal/states/factories.go` | `NewSettingsMenuState` ファクトリー追加 |
| `internal/states/main_menu.go` | 「設定」項目を追加し `SettingsMenuState` を `TransPush` |
| `internal/states/main_menu_test.go` | メニュー項目追加に伴う期待値の更新 |
| `internal/states/golden_test.go` | `TestGolden_SettingsMenu` 追加、MainMenu golden 更新 |

（初版で追加した解像度プリセット・`nextResolution`・`applyResolution` などの解像度切り替えロジックは撤回し、設定画面は土台のみ残した。）

## 採用しなかった方法

- **解像度プリセットUI＋論理解像度変更**: `Layout()` が選択解像度を返し、設定メニューでプリセットを切り替える方式。実装したが、32x32 ピクセルアートでは論理解像度を上げても情報量が増えず**視野が広がるだけ**で、ゲームバランスに影響する。ユーザーの用途に合わないため撤回した
- **解像度プリセットUI＋ウィンドウスケーリング（論理固定）**: 論理解像度を固定しプリセットをウィンドウサイズとする方式。ウィンドウを拡大縮小するだけで**見た目が変わらず**、それは Ebiten のウィンドウリサイズと同じことなので、専用のプリセットUIを設ける意味がない
- **ドラッグしたウィンドウサイズの永続化**: リサイズ後のサイズを `settings.toml` に保存して次回復元する案。今回の要件（可変にするだけ）には含まれず、YAGNI のため見送る。必要になれば `config.UserConfig` に保存する形で追加できる
- **言語を左右キーで循環（`← 値 →`）**: 設定項目上で左右キーを押すたびに言語を切り替える方式。当初実装したが、区切りに使った矢印記号（`◀ ▶`）がフォント未収録で文字化けした。フォントにある `← →` へ替えても、値の選択を Enter モーダルにする方が既存の宣言的選択肢（`messagedata.WithChoice`）を再利用でき、記号にも依存しない。左右循環は廃止した

## コメント

- ウィンドウの可変化は Ebiten の `WindowResizingModeEnabled` で既に実現されており、追加実装は不要だった。今回残す変更は `ScreenDimensions` の固定（潜在バグ修正）のみである
- `config.UserConfig.WindowWidth/Height` は「初期ウィンドウサイズ」として残す。論理解像度（`consts` 固定）とは別概念である
- 言語設定は仮であり、`config.UserConfig.Language` に保存するだけで実際の表示切り替え（i18n）は未実装である。i18n を実装した際にこの保存値を参照する
- グローバル設定画面には、音量など将来の設定をこの画面の項目として追加していく。設定値の永続化基盤（`20260708_47.md`）はそのまま利用できる
- テストは Makefile と同様に bwrap + xvfb-run 経由で実行する。素の `go test` は実ディスプレイでウィンドウを開き、ゴールデン比較が環境差で失敗するため使わない

## 進捗

- [x] `internal/maingame/game.go`: `SetScreenDimensions` を論理解像度に固定
- [x] 解像度切り替えロジック（プリセット・`nextResolution`・`applyResolution`）を撤回
- [x] `internal/config/config.go`: `UserConfig` に `Language` を追加
- [x] `internal/states/settings_menu.go`: 設定画面と言語設定（仮）を実装
- [x] `internal/states/main_menu.go`: 「設定」項目を追加
- [x] テスト・ゴールデン（SettingsMenu 追加、MainMenu 更新）
- [x] `make check` 通過
- [x] `make test` 通過
