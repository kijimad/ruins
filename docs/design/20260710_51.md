---
status: in-progress
tags: []
auto: needs-decision
---

# ECSエンジンの goecs から Ark への移行

## 背景

ゲームは ECS エンジンに `github.com/x-hgg-x/goecs`（以下 goecs）を使っている。goecs のコンポーネント格納は `data []interface{}`（実測確認済み）で、**全コンポーネントを interface{} にボクシング**する。このため:

- `SliceComponent.Get(entity)` が `interface{}` を返し、呼び出し側で `.(*gc.X)` の手書き型アサーションが必要。
- 直近で導入した `Component[T]` ラッパー（`docs/design/20260709_50.md`）はこの untyped なエンジンの上に被せた**緩和策**であり、根本原因（エンジンが untyped）を解消したものではない。
- ボクシングによりヒープ確保とキャッシュ効率の劣化がある。

`Component[T]` ラッパーで型安全は達成したが、より根本的な対応として、型安全（generics ネイティブ）・archetype 格納（ボクシング撤廃）・シリアライズ簡素化を備えた後継エンジンへの移行を検討した。

候補として Arche（`mlange-42/arche`）と、その後継 Ark（`mlange-42/ark`、同作者・generics-first・より高速）を比較し、**Ark（v0.8.3）+ ark-serde（v0.3.2）** を対象に PoC（スパイク）で実現性を検証した。

### スパイクの結論（`internal/archespike/`、本体非依存）

移行の二大懸念（save 層の載せ替え、systems の書き換え）がともに解消できることを実地で確認した。

1. **save 往復**: `arkserde.Serialize(world)` でワールド丸ごとJSON化、`Deserialize` で復元。**コンポーネント内の素の `ecs.Entity` フィールドも serde が自動で再マッピング**する（現状の stableID 手動再マップが不要）。決定性（同一ワールド→同一JSON）も確認。
2. **システム移植**: `camera_system` の追従ロジックを Ark の Filter/Query に移植。`Join(...).Visit(...)` → `NewFilterN(world).Query()` + `for q.Next()` は**ほぼ機械的な1:1変換**。

## 要件

- コンポーネントアクセスを generics でネイティブに型安全化する（`interface{}` ボクシングを撤廃）。
- save/load を ark-serde ベースに載せ替え、手書きの per-component 変換を撤廃する。
- 段階移行とし、各段階で `make check`（全テスト + lint）が通ること。
- セーブ形式は変わってよい（開発中でリリース済みセーブは無い）。ただし移行完了時点で往復（roundtrip）と決定性のテストを担保する。

## 現状の結合度（移行対象の物量、実測）

| 対象 | 数 |
|---|---|
| `Manager.Join` | 160 |
| `.Visit` | 121 |
| `AddComponent` | 1104 |
| `HasComponent` | 374 |
| `ecs.Entity` 参照 | 590 |
| `NewEntity` | 570 |
| goecs を import するファイル | 116 |
| save 層の依存 | 251 |
| 手書き `toSaveData`/`fromSaveData` | 44 |
| oapi `SaveData*` スキーマ型 | 49 |
| `save/*.go` 行数（非テスト） | 約3,475 |

パラダイム的な未知は PoC で解消済み。残るコストは主に**この物量の機械的変換**。

## 設計

### 方針: 薄い互換層を挟んで段階移行する

一括置換は 116 ファイルに及び高リスクなため、`w.World` が Ark の `*ecs.World` を保持する薄い互換層を用意し、パッケージ単位で移行する。

### API 対応表（goecs → Ark）

| goecs | Ark |
|---|---|
| `manager.NewEntity()` + `AddComponent` | `ecs.NewMapN[...](world).NewEntity(&a, ...)` |
| `world.Components.X.MustGet(e)` | `mapX.Get(e)`（`ecs.NewMap1[X](world)`） |
| `world.Components.X.TryGet(e)` | `mapX.GetUnchecked` + Has 判定、または filter |
| `entity.HasComponent(X)` | `mapX.Has(e)` |
| `Join(A, B).Visit(func(e){...})` | `ecs.NewFilterN[A,B](world).Query()` + `for q.Next(){ a,b := q.Get() }` |
| マーカー（NullComponent） | ゼロサイズ struct コンポーネント |
| save: 44関数 + 49スキーマ型 | `arkserde.Serialize`/`Deserialize` |
| エンティティ参照の stableID 手動再マップ | serde が自動再マッピング |

### コンポーネントの扱い

- コンポーネントは Ark 上ではプレーンな struct（値型）として archetype に格納される。現状の `gc.X` 型は概ねそのまま使える。
- Ark はグローバルにコンポーネント型を登録するモデルのため、現状の `Components` 構造体（コンポーネントハンドルの束）は不要になり、各所で `ecs.NewMapN[...]` / `ecs.NewFilterN[...]` を使う。Filter/Map は生成コストがあるためシステム側でキャッシュする。

### 監査が必要なコンポーネント（JSON化できない要素）

serde は JSON マーシャルのため、以下は `json:"-"` 除外か非保存で対応する（現状の save でも同様に除外/特別扱いしている）。

- map を持つもの（例: `spatial_index` の `Characters map[GridElement]ecs.Entity`）。
- interface を持つもの（例: `StateChangeRequest`、`AI.Planner`）。
- 関数フィールドを持つもの（あれば）。

## 変更箇所

| ファイル/範囲 | 変更内容 |
|---|---|
| `go.mod` | `mlange-42/ark`, `mlange-42/ark-serde` を追加、goecs を最終的に削除 |
| `internal/world`（w.World 定義） | Ark `*ecs.World` を保持する互換層を追加 |
| `internal/save/*` | ark-serde ベースに載せ替え、converter.go の44関数・oapiの49スキーマ型を撤去 |
| `internal/systems/*`, `internal/activity/*` 他 | `Join/Visit`→`Filter/Query`、`AddComponent`→`Map.NewEntity/Add` に移行 |
| `internal/components/components.go` | `Component[T]`/`Components` 構造体を撤去（Ark はグローバル登録のため不要） |
| `internal/archespike/` | 検証用スパイク（移行完了時に削除） |

## 採用しなかった方法

- **`Component[T]` ラッパーのまま（現状維持）**。型安全は達成済みで低コストだが、ボクシング撤廃・save 簡素化は得られず、untyped エンジンという根本は残る。将来機能（relations 等）も無い。ゲームの規模（ターン制・最大〜1000エンティティ）では性能は当面問題にならないため、「移行しない」も合理的な選択肢。移行の主駆動は**save 簡素化と土台の健全化**であり、性能ではない。
- **Arche（`mlange-42/arche`）**。Ark の前身。API がやや冗長で、後継の Ark があるため新規移行先としては非推奨。
- **donburi**（ebiten 界隈で実績のある別 ECS）。generics で型安全寄りだが、archetype 性能と serde の完成度で Ark を上回る決め手が無い。
- **一括ビッグバン移行**。116ファイルを一度に置換するのは高リスク。互換層を挟んだ段階移行を採る。

## コメント

- 当初 save 層を「移行の最難関」と見立てていたが誤りだった。PoC の結果、save は**むしろ大幅に簡素化される**（44関数+49スキーマ型+手動再マップが消える）側で、これが Ark 移行の最も強い動機になる。
- 型安全は `Component[T]` ラッパーで既に取得済みのため、Ark 移行で得られる型安全の増分は小さい。移行を正当化するのは主に「save 簡素化 + ボクシング撤廃 + 土台の健全化」。
- 移行するか否かは**投資判断**。しない場合もラッパーで型安全は保たれる。する場合は段階移行で物量を捌く。
- スパイク（`internal/archespike/`）は本体に一切依存しないため、判断が「見送り」でも安全に破棄できる。

## 追記: serde 監査結果と最終決定

Phase 1 の監査（JSON化できない要素の洗い出し）を先行実施し、実データで以下を確定した。

- **map キー型**: `SkillID`/`ElementType`/`ModifierKey` は string のため `map[...]V` は serde OK。**struct キー（`GridElement`/`Position`）のみ不可**。該当は視界情報（`ExploredTiles`/`VisibleTiles`/`BlockPass`/`Characters`）で、いずれも**一時状態＝save 対象外**（ロード時に再構築）。
- **interface フィールド**: `AI.Planner PlannerConfig` が save 対象で唯一の実ブロッカー。ark-serde は interface を復元できない（実測: `cannot unmarshal ... into ... interface`）。`Interactable.Interactions []InteractionData` も interface スライスだが save 対象外。

結論として、whole-world serde の実ブロッカーは **`AI.Planner`（interface）だけ**に狭まった。

### 最終決定: AI.Planner を平坦化したうえで Ark へ移行する

`AI.Planner interface` を**プレーンデータの別コンポーネント**（`SoloAI` / `SquadAI` を独立コンポーネント化）に分割する。振る舞い（`ReactToHostile`/`ResetCombat`）は aiinput の system 側へ移す。これは ECS のデータ指向原則（コンポーネントは振る舞いを持たないデータ）に沿った健全化であり、副次的に serde 互換になる。平坦化は goecs 上で先行実施し、その後 Ark へ移行する。

一時状態コンポーネント（SpatialIndex・視界 map）は save 対象外として `json:"-"` 除外する。

## 進捗

### Phase 0: PoC（完了）

- [x] Ark + ark-serde を導入し、save 往復（エンティティ参照の自動再マップ・決定性）を実証
- [x] 代表システム（camera）の Filter/Query 移植を実証
- [x] serde 監査（interface / struct キー map の実ブロッカー特定）

### Phase 1: interface コンポーネントの平坦化（goecs 上で先行・green で刻む）

Ark は具体型で archetype 格納するため、interface を直接格納するコンポーネントを
全て平坦化してから中核に入る。

**前提1a: AI.Planner（完了）**
- [x] `SoloAI`/`SquadAI` を独立コンポーネント化（`AI{Planner}` を廃止）
- [x] dispatch を Join(SoloAI)/Join(SquadAI) に、ReactToHostile を両型 comma-ok に
- [x] save の aiToSaveData を solo/squad 別に分割
- [x] `make check` 通過（コミット済）

**前提1b: StateChangeRequest（完了）**
- [x] interface + イベント構造体 → Kind 判別子のタグ付きユニオン + コンストラクタ
- [x] RequestStateChange/ConsumeStateChange をポインタ意味論に、dungeon.go を Kind switch に
- [x] `make check` 通過（コミット済）

**確認: 直接 interface を格納する他のコンポーネントは無し**
- VisualEffect/InteractionData/FactionType/Pred/Attacker/Location/Amounter はいずれも
  struct 内部フィールドかヘルパ、または ECS では具体型（Faction は3マーカー）で格納。
  Ark 格納の直接ブロッカーは AI.Planner と StateChangeRequest のみで、両方解消済み。
- 残る FactionType の EntitySpec 変換（Stringer reflect）は中核のエンティティ生成再設計で対応する。

### 中核の確定設計（Ark API 実地確認済み）

Ark v0.8.3 のソースを確認し、以下を確定した。

**World 保持**
- `w.World` は `*ecs.World`（Ark）を保持。`Manager`/`GetManager` は撤去。
- `NewEntity`/`RemoveEntity`/`Alive` は `*ecs.World` のメソッド。

**Components 構造体は残す（doc当初の「撤去」から方針変更）**
- 各フィールドを型付き `*ecs.Map[T]`（`ecs.NewMap[T](world)`）に。`Map[T]` は
  `Get(e)*T` / `Has(e)bool` / `Add(e,*T)` / `Set(e,*T)` / `NewEntity(*T)` / `Remove(e)` を持つ。
- これにより `world.Components.X.Get(e)` が `*gc.X` を直接返し（`.(*gc.X)` 削除）、
  移行が最小churnで済む。Filter は各所で `ecs.NewFilterN[...]` を使う（システム側でキャッシュ可）。
- 型マッピングの例外: `DungeonState→Dungeon`, `VisualEffect→VisualEffects`,
  `FactionAlly/Enemy/Neutral→Faction*Data`, `Boss→新規 type Boss struct{}`。
- `InitializeComponents` はリフレクション不可（generics 実体化のため）。66行の
  `c.X = ecs.NewMap[X](world)` を手書き。

**エンティティ生成機構（`engine/entities`）— concrete-type で uniform 化**
- `ecs.TypeID(w, reflect.Type) ID`（公開）＋ `w.Unsafe()` で動的生成する。ark-serde の
  deserialize と同一手法（`u.Add(e,id)` → `dst:=u.Get(e,id)` → `reflect.NewAt(t,dst).Elem().Set(v)`）。
- EntitySpec の各フィールド（すべて `*T`）を deref し、interface（`FactionType`/`LocationType`）は
  さらに `.Elem()` で**具体型**へ剥がす。struct/string/interface すべて `reflect.TypeOf(具体値)`
  に統一でき、旧コードの `String()` 名前マッチは**不要**になる（機構が単純化）。

### 段階戦略（レイヤ順・下から green 化）

原子的と言っても World/Components/Entity の依存順に下から積める。

- **レイヤ0: `internal/components`**（world 非依存）。Components を Map 化＋Boss型＋invariant.go 移行。
  `go build ./internal/components` 単独で green 化・コミット可能。← 最初の検証可能マイルストーン
- **レイヤ1: `internal/engine/entities`**。生成機構を TypeID/Unsafe に。
- **レイヤ2: `internal/world`**。world.go を Ark 保持に。
- **レイヤ3: 残り約2963箇所**（Join/Visit→Filter/Query、AddComponent→Map.Add、HasComponent→Map.Has、
  NewEntity/DeleteEntity）。ここが原子的赤ビルドの本体。並列エージェントでパッケージ単位に貫通。
- **レイヤ4: save**。ark-serde 化、converter 44関数＋oapi 49型を撤去。

### Phase 1: 基盤（中核レイヤ0-2 完了）

- [x] レイヤ0: components を Map 化（Boss型追加、InitializeComponents 手書き、invariant.go を hasser 抽象化で移行）。`go build ./internal/components` green
- [x] レイヤ1: entities 生成機構を TypeID/Unsafe に（`go build ./internal/engine/entities` green）
- [x] レイヤ1.5: engine/components の InitComponents/ComponentInitializer を `*ecs.World` に
- [x] レイヤ2: world.go を Ark `*ecs.World` 保持に（`InitSingleton` は `Components.X.Add`）
- [x] レイヤ3: 残り約2963箇所の一括移行（resources 含む）＋実行時ギャップ修正で全テスト green
- [ ] JSON化できないコンポーネント要素（map/interface/関数）の監査と save 除外対応

**レイヤ3 の機械変換ルール（goecs → Ark）**

| goecs | Ark |
|---|---|
| `ecs "github.com/x-hgg-x/goecs/v2"` | `"github.com/mlange-42/ark/ecs"` |
| `world.Manager.NewEntity()` | `world.World.NewEntity()` |
| `world.Manager.DeleteEntity(e)` | `world.World.RemoveEntity(e)` |
| `entity.AddComponent(world.Components.X, &d)` | `world.Components.X.Add(entity, &d)` |
| `entity.HasComponent(world.Components.X)` | `world.Components.X.Has(entity)` |
| `entity.RemoveComponent(world.Components.X)` | `world.Components.X.Remove(entity)` |
| `world.Components.X.Get(e).(*gc.X)` | `world.Components.X.Get(e)`（アサーション削除） |
| `world.Manager.Join(A,B).Visit(ecs.Visit(func(e){...}))` | `q := ecs.NewFilterN[...](world.World).Query(); for q.Next(){ e:=q.Entity(); ... }` |
| マーカー追加 `AddComponent(C.Player, &ecs.NullComponent{})` | `C.Player.Add(e, &gc.Player{})` |
| `world.GetManager()`/`GetComponents()` | 廃止。`world.World` を直接使用 |

注意: `ecs.Entity` はArkでも同名だが goecs では `int`、Ark では struct。`==` 比較や 0値
判定は Ark の `ecs.Entity{}` / `.IsZero()` を使う。Filter/Query は生成コストがあるため
システムのフィールドにキャッシュするのが望ましい（初回は inline でも可）。

### 中核〜レイヤ3 完了: 全コード Ark でコンパイル GREEN

- [x] レイヤ3 機械変換（AddComponent/HasComponent/Get 等 約2663箇所を正規表現で一括）
- [x] Join/Visit → Filter/Query（56ファイル・約119箇所、並列エージェント）
- [x] スイープ由来バグの掃討（修飾子受け手破損・再アサーション・marker具体型化・Faction変数形）
- [x] 基盤の型ギャップ（InvalidEntity/GetSingleton/component_debug/mergeStackable/comma-ok Get）
- [x] `go build ./internal/...`（非テスト）GREEN、`go test -run=NONE ./internal/...`（テスト）GREEN
- [x] 実行時ギャップ修正: 12 パッケージの panic を全解消し **`make test` 全41パッケージ PASS・`make check`（lint含む）GREEN**

### 完了: Ark 意味論ギャップの実行時修正（tests が検証ハーネス）

Ark は goecs と異なり、不在・重複時に**パニックする**。goecs は寛容だった。
tests をハーネスにパッケージ単位で潰した。基盤（query/lifecycle）から下流へ green 化し、
独立パッケージは並列エージェントで処理した。

| goecs（寛容） | Ark（厳格） | 対処 |
|---|---|---|
| `Get(e)` 不在→ `nil` 返却 | `Get(e)` 不在→ **panic** | `if C.X.Has(e) { ... C.X.Get(e) }` でガード。`x := Get(e); if x != nil` は死にコードなので Has 判定に書き換え |
| `Add(e,d)` 既存→上書き/追加 | `Add(e,d)` 既存→ **panic** | 冪等更新は `gc.Upsert(C.X, e, d)`（Has判定でAdd/Set使い分け・新設ヘルパ）。マーカーは `if !C.X.Has(e){ Add }` |
| `Remove(e)` 不在→ 無視 | `Remove(e)` 不在→ **panic** | `if C.X.Has(e) { C.X.Remove(e) }` |
| `Has(e)`/`Get(e)` 死亡→ 素通り | 死亡エンティティ→ **panic** | 削除確認は `world.World.Alive(e)`。Has/Get 前に Alive ガード |
| Join 反復中の削除 OK | Query 反復中は**ワールドロック**、構造変更で panic | 対象を slice に集めてから反復後に変更（collect-then-mutate） |
| 死亡エンティティ二重削除 | `RemoveEntity` 二重→ panic | `if world.World.Alive(e) { RemoveEntity(e) }` |
| **ポインタ格納**（Get したポインタが永続） | **値コピー格納**（`Add/Set` は `*a=*comp` でコピー） | Add/Set 後にローカルポインタを変更しても格納側に届かない。変更を反映・観測するには `stored := C.X.Get(e)` で**格納側ポインタを取り直す**。`StartActivity`・`LockAllDoors` の状態書き込みバグの真因 |
| （同上） | **Get ポインタは構造変更で失効** | `RemoveEntity`/`Remove`/`Add` 等の archetype 変化で Get 済みポインタが無効化される。削除前に値を退避（`copied := *Get(e)`）、または操作後に取り直す |

最後の2行（値コピー・ポインタ失効）は当初の見立てに無く、実行時修正で判明した最重要ギャップ。
表層 panic だけでなく、状態遷移が静かに失われる**サイレントな production バグ**を生んでいた
（`StartActivity` の `SetActivity` 後の `comp.State=Running`、`LockAllDoors` の `Locked=true`）。

**解消済みパッケージ**（元 panic 12個・全 green）: activity, aiinput, mapplanner, raw, save,
states, systems, widgets/messagelog, widgets/tabmenu, world/gameaction, world/lifecycle, world/query。
golden 系（messagelog/tabmenu/states）は `make test`（xvfb環境）必須で、bare `go test` は
レンダリング差分で誤検知するため切り分けに注意。

### Phase 2: save 載せ替え（完了）

- [x] save/load を ark-serde ベースに置換（`serde.go` 新設、`manager.go` を766→291行に）
- [x] converter.go(44関数)・stable_id.go(手動再マップ)・validate.go(oapi検証) を撤去
- [x] 往復テストを Ark 版で担保（`serde_test.go`。`SaveWorld`/`LoadWorld` 経由）
- [ ] oapi の SaveData* スキーマ型（未使用・354行相当）の撤去（後続）

**保存モデルの決定: 選択的保存 → 丸ごと保存**
- 旧: プレイヤー/隊員/インベントリのみ保存、ロード時にダンジョン再生成。
- 新: `arkserde.Serialize(world)` でワールド全体を保存、ロード時は全復元（敵・地形・扉も）。
  ark-serde の除外は**型単位**（`SkipComponents`）で**エンティティ単位の選択は不可**なため、
  選択的保存は serde と両立しない。ユーザー判断で丸ごと保存へ変更した。

**実装上の確定事項（実測）**
- Deserialize は `world.World.Reset()` 済みワールドを要求する。
- singleton はロード後に GameLog/SpatialIndex を再付与し `Resources.SingletonEntity` を張り直す。
- serde ブロッカーは `ProvidesHealing.Amount`(Amounter interface) のみ。タグ付きデータへ平坦化して解消。
- 一時状態11種を `SkipComponents`、Dungeon視界map・SpriteRender.Options・CharModifiers.Sources を `json:"-"`。
- ark-serde v0.3.2 の出力はマップ反復順で**非決定的**。checksum は保存バイト列に対して計算・検証するため
  整合性は保たれる。旧 StableID 方式の決定性は仕様上不要（セーブ形式は変わってよい）。
- `extractPlayerName` はクエリ全反復してから返す（途中returnはワールドをロックしたまま残す）。

### Phase 3: systems 移行（レイヤ3で完了済み）

- [x] `Join/Visit`→`Filter/Query`、`AddComponent`→`Map` はレイヤ3スイープ＋実行時ギャップ修正で完了
- [x] 全パッケージ `make test` 通過

### Phase 4: goecs 撤去

- [x] goecs 依存を go.mod から削除（`go mod tidy`。arche/yourbasic も撤去、ark/ark-serde を直接依存化）
- [x] goecs の import 実体はレイヤ3で全撤去済み（`Component[T]` は Ark 移行で `*ecs.Map[T]` に置換済み）
- [x] `internal/archespike/`（PoC）の削除
- [ ] oapi SaveData* スキーマ型の撤去（TypeSpec→openapi.yml→models.gen.go の再生成が必要。未使用・無害なため後続）
- [x] `make check` 通過（0 issues、全テストPASS）
