---
status: done # draft|accepted|in-progress|done|superseded|dropped
tags: [steam, meta] # 領域ラベル
auto: needs-decision # mechanical=無人着手可 / needs-decision=人の判断必須
---

# panic 時のスタックトレースをファイルへ保存するクラッシュレポート

## 背景

配布ビルドで落ちたとき、原因を追う手掛かりが残らない。プレイヤーの端末で起きた panic は
標準エラー出力へ流れて消え、こちらには届かない。落ちた瞬間のスタックトレースを端末上の
ファイルへ残せば、後から回収して原因を追える。

Steam にはクラッシュ報告 API があるが本ゲームでは使えない。`SteamAPI_WriteMiniDump` は
Windows 32bit 限定で、C++ の構造化例外を `_set_se_translator` でフックする設計であり、
Go の panic を捕捉しない。バインディング `internal/steam` も `Init` と `GameLanguage`
だけを公開しており MiniDump 系を持たない。よって外部機構に頼らず、Go の `recover` と
`runtime/debug.Stack` で自前にファイルへ保存する。

`t.Fatal` の禁止は既に達成済み。`.golangci.yml` の `forbidigo` が型情報付きで
`testing.(T|B|TB).(Errorf?|Fatalf?)` を弾き、現状の使用は 0 件。本ドキュメントの対象外。

## 要件

- panic 発生時に、時刻・panic 値・スタックトレース・ビルド情報を**構造化 JSON レコード**として
  ファイルへ1件ずつ残す。既存 `internal/logger` の JSON エントリと同じ命名に揃え、後から機械的に
  集計・検索できるようにする。
- ゲーム由来の panic を取りこぼさない。Ebiten がどの goroutine で呼んでも捕らえる。
- 握りつぶさない。保存後は本来どおりプロセスを落とす。壊れた状態で続行させない。
- desktop のみ。WASM は体験版でファイルシステムが無いので何もしない。
- 保存先はセーブ・設定と同じ `os.UserConfigDir()/ruins/` の下。プレイヤーが辿れる。

## 設計

### なぜ自前コールバックの先頭へ defer を置くか

`recover` は同一 goroutine の panic しか捕らえない。Ebiten の既定のマルチスレッド構成では、
`Update`/`Draw`/`Layout` は `RunGame` の呼び出し元とは別の専用ゲーム goroutine で回る。ebiten
v2.10.2 の `internal/ui/ui_glfw.go` の `runMultiThread` が errgroup で `loopGame` をゲーム
goroutine として起こし、その中で3つを呼ぶ。RunGame doc の「game's functions are called on the
same goroutine」は3つが互いに同じ goroutine という意味で、`runPlay` と同じではない。desktop の
ループに recover は無く、mobile だけが recover する。

ゆえに `runPlay` 頂上の Guard はゲーム由来の panic に届かない。別 goroutine だからだ。ゲーム由来を
取りこぼさない急所は自前 `MainGame` の各コールバックの先頭で、Go の defer 意味論によりその呼び出し内
の panic は必ずその defer へ巻き戻る。`runPlay` 頂上の Guard は `RunGame` に入る前の初期化フェーズ
の panic を受け持つ。

`cgo`/GL のネイティブな segfault は Go の defer が走らないので捕らえられない。これは
コアダンプの領分で、本機構の対象外とする。

### パッケージ crashreport

```go
package crashreport

// saveOnce は1プロセスで保存を1回に限る。Guard は必ず通る関数ごとに置くので、同一 goroutine で
// 入れ子になった Guard が再 panic を再捕捉しても、クラッシュ1件につきファイルは1つに保たれる。
var saveOnce sync.Once

// userConfigDir は保存先の基底を解決する。既定は os.UserConfigDir。テストが一時ディレクトリへ
// 差し替えられるようパッケージ変数にする。これが唯一の保存先注入経路。
var userConfigDir = os.UserConfigDir

// SetStateProvider は最上位ステート名の取り出し方を登録する。起動時に1度呼び、Save が
// クラッシュ時にこれを引く。Guard の呼び出し側を増やさず、毎フレームの更新も要らない。
func SetStateProvider(f func() string)

// Guard は defer で使う。panic を捕らえて1回だけ Save し、握りつぶさず再 panic する。
// 同一 goroutine の panic だけを捕らえるので、必ず通る関数の先頭へ置く。
func Guard() {
	if r := recover(); r != nil {
		saveOnce.Do(func() { Save(r, debug.Stack()) })
		panic(r) // 保存後に本来の落ち方へ戻す。stderr へのトレース出力と非0終了を保つ
	}
}

// Save はクラッシュ情報を1ファイルへ書く。desktop のみ。
// WASM はファイルシステムが無いので何もしない。ディレクトリ作成と書き込みの失敗は握りつぶす。
// クラッシュ処理でさらに落ちて元の panic を覆い隠さないため。
func Save(recovered any, stack []byte)
```

`saveOnce` は「1プロセスにつきクラッシュファイルは1つ」という不変条件を担う。Ebiten のソース確認では、
コールバックはゲーム goroutine で順に呼ばれて入れ子にならず、`runPlay` の Guard は別 goroutine なので
再捕捉もしない。よって現構成で二重保存は起きない。それでも `saveOnce` を置くのは、Ebiten の
スレッド構成という内部事情に正しさを依存させないため。置き場が増えても構成が変わっても、ユーザーが
1クラッシュで複数ファイルを見て戸惑うことがない。コストは `sync.Once` 1つで無視できる。

### 保存先とレコード形式

保存先はセーブ・設定と共通の親の下。1クラッシュ1ファイルにして、時刻をファイル名へ入れる。
上書きされず履歴が残り、クラッシュ中の追記競合や書きかけ混在も起きない。

ファイル名の時刻はコロンを使わない。Windows はファイル名にコロンを許さず、Steam 配布の主対象が
Windows のため。秒未満まで入れて同秒の衝突を避ける。

```go
// <os.UserConfigDir()>/ruins/crash/crash-<YYYYMMDD-HHMMSS-nanos>.json
// 例 crash-20260916-000431-123456789.json
```

ファイルの中身は構造化 JSON レコード1件。フィールド名は既存 `internal/logger` の JSON
エントリ（timestamp・level・category・message）に揃える。スタックトレースは1つの文字列
フィールドに収め、レコード全体を1オブジェクトに保つ。

```go
// CrashRecord は1回のクラッシュを表す構造化ログレコード。
type CrashRecord struct {
	Timestamp string `json:"timestamp"`       // RFC3339
	Level     string `json:"level"`           // logger.LevelFatal.String() を代入。値は "FATAL"
	Category  string `json:"category"`        // "crash"
	Message   string `json:"message"`         // recover した値の文字列。全文は Stack にある
	Version   string `json:"version"`         // ビルド版
	GOOS      string `json:"goos"`            // runtime.GOOS
	GOARCH    string `json:"goarch"`          // runtime.GOARCH
	Steam     bool   `json:"steam"`           // consts.IsSteamBuild
	State     string `json:"state,omitempty"` // provider 由来の最上位ステート名。無ければ空
	Stack     string `json:"stack"`           // debug.Stack の文字列
}
```

`message` と `panic` を分けない。前者は panic 値、後者も recover 値で実質重複するため、panic 値は
`message` 1つに収め、logger の JSON エントリが必ず持つ `message` へ素直に載せる。トレース全文は
`stack` にある。`level` はリテラルでなく `logger.LevelFatal.String()` を代入し、logger の表記を
単一出典にする。fatal と FATAL のような取り違えを構造的に防ぐ。

`Save` はこの構造体を組み立て、`json.NewEncoder(file).Encode` で書く。加えて、コンソールにも
気づけるよう既存 logger で Error レベルの短い1行を出し、保存先パスを添える。全文はファイル、
道標は通常のログ経路、と役割を分ける。

### 設置

`MainGame` の Ebiten コールバック3つと、初期化フェーズの頂上へ置く。

```go
func (g *MainGame) Update() error {
	defer crashreport.Guard()
	// ...
}
func (g *MainGame) Draw(screen *ebiten.Image) { defer crashreport.Guard(); /* ... */ }
func (g *MainGame) Layout(w, h int) (int, int) { defer crashreport.Guard(); /* ... */ }
```

`debug.SetTraceback("all")` は `main.go` の最初で1度呼ぶ。設定読み込みより前の初期化で落ちても
全 goroutine のトレースが stderr に残るよう、プロセス起動直後の最早期に置く。`runPlay` の中だと
設定・ウィンドウ設定より後になり、それ以前の panic のトレースが薄くなる。

落ちた時点のステート名は `Guard` の引数を増やさず、起動時に `crashreport.SetStateProvider` へ
最上位ステート名の取り出し方を1度登録し、`Save` がクラッシュ時にそれを引いて `State` に載せる。
push でなく pull にすることで毎フレームの更新が要らず、配線は起動時の1箇所で済む。`omitempty` なので
未登録でも無害。provider が落ちても握りつぶし、元の panic を覆わない。

### フロー

Guard は関数ごとに別々に置かれる独立した defer で、ゲーム goroutine 側と runPlay 側は別インスタンス。
共有するのは `saveOnce` だけ。

```mermaid
flowchart TD
    U[MainGame.Update/Draw/Layout<br/>ゲーム goroutine] -->|panic| G1[defer crashreport.Guard]
    I[runPlay 頂上・初期化フェーズ<br/>main goroutine] -->|panic| G2[defer crashreport.Guard]
    G1 --> O{saveOnce 未実行?}
    G2 --> O
    O -->|はい| S[Save: CrashRecord を crash-.json へ]
    O -->|いいえ 既に保存済み| R
    S --> B[logger で保存パスを Error 出力]
    B --> R[再 panic]
    R -->|未捕捉で頂上へ| T[main.go の SetTraceback all で全 goroutine を stderr へ]
```

## 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/crashreport/crashreport.go` | 新規。`Guard`/`Save`/`SetStateProvider`・`CrashRecord`・`buildRecord`・`saveOnce`・logger への道標 |
| `internal/crashreport/crashreport_desktop.go` | 新規。`//go:build !js`。`userConfigDir` seam・`writeRecord`/`writeRecordTo` でファイルへ JSON を書く |
| `internal/crashreport/crashreport_wasm.go` | 新規。`//go:build js`。`writeRecord` を no-op にする |
| `internal/crashreport/doc.go` | 新規。パッケージの doc |
| `internal/logger/category.go` | `CategoryCrash` を追加。道標の1行に使う |
| `main.go` | 起動最初期に `debug.SetTraceback("all")` |
| `internal/maingame/game.go` | `Update`/`Draw`/`Layout` の先頭へ `defer crashreport.Guard()` |
| `internal/cmd/play.go` | `runPlay` 頂上へ `defer crashreport.Guard()`。ステート機械作成後に `SetStateProvider` を1度登録 |
| `internal/crashreport/crashreport_test.go` | 新規。純関数の `buildRecord`/`writeRecordTo` を並列で、`Guard`/`currentState` の seam 差し替えを直列で検証 |

## 採用しなかった方法

- **Steam の `WriteMiniDump`**。Windows 32bit・C++ SEH 前提・非 C++ ランタイム非対応で Go の
  panic を捕らえない。EOL 間近でもある。バインディングにも無い。動機であって手段にはならない。
- **実行ファイルと同じディレクトリへ保存する**。単純で見つけやすいが、出荷ビルドの実行ディレクトリは
  Windows の `Program Files` など UAC 保護で書き込み不可なことが多い。クラッシュがまさに要る出荷
  ビルドで書けない。`UserConfigDir` は必ず書けてセーブ・設定と同じ親で一貫する。発見性は道標の
  ログ1行で補う。
- **`RunGame` を1つの Guard で包むだけにする**。ソース確認の結果これは不可能。Ebiten は既定で
  `Update`/`Draw`/`Layout` を専用ゲーム goroutine で回すので、`runPlay` の Guard は別 goroutine
  となり届かない。各コールバック先頭の Guard が唯一の受け皿。
- **panic を握りつぶして続行する**。壊れた状態で走らせると二次被害と再現性の喪失を招く。保存後は
  再 panic して本来どおり落とす。
- **fd 2 をファイルへ複製して stderr ごと保存する**。Ebiten はゲーム goroutine のほかにレンダー
  スレッド goroutine を持ち、そこや内部 goroutine の未捕捉 panic は Guard では拾えない。fd 2 の
  リダイレクトなら自動で入る。ただしこれらは GL・エンジンのバグで極めて稀な一方、費用は Linux の
  `Dup3`・Darwin の `Dup2`・Windows の `SetStdHandle` を build tag で分ける OS 別実装に、dev の
  コンソール出力を潰さないゲートまで要る。稀なエンジン panic のために重すぎるので採らない。ゲーム
  ロジックの panic は Guard が構造化 JSON で捕らえており、バグの主戦場は覆えている。
- **人が読むプレーンテキストで書く**。読みやすいが機械集計しにくい。構造化 JSON にして、
  既存ログと同じ道具で検索・集計できるようにする。
- **既存 logger でファイルへ全文を出す**。logger は出力先が stdout に固定で、端末に残る
  ファイルにならない。ここへ file シンクを足すのは logger 全体の改修になり本題から広がる。
  クラッシュは専用ファイルへ自前で JSON を書き、logger へは道標の1行だけ流す。
- **`log/slog` を新規に導入する**。標準の構造化ロガーで file シンクも持つが、本プロジェクトは
  既に自前の構造化ロガーを運用している。系を2つに増やさず、既存の JSON エントリの形へ揃える。
- **ログローテーション**。`Guard` は保存後に再 panic してプロセスを落とすので1起動1ファイルで、
  クラッシュ自体も稀。件数上限の間引きすら過剰と判断し、ローテーションは持たない。増え方が問題に
  なれば後から件数キャップを足せる。

## コメント

- 再 panic か `os.Exit` かは再 panic を採る。stderr への標準トレースと非0終了という既定の
  落ち方をそのまま残せるため。
- 保存先の作成失敗や書き込み失敗は握りつぶす。クラッシュ処理でさらに落ちて元の panic を
  覆い隠すのが最悪なので、保存はベストエフォートにする。

## 進捗

- [x] `internal/crashreport` パッケージ。`CrashRecord`・`Save`・`buildRecord`・`Guard`・`saveOnce`・logger 道標
- [x] desktop の `writeRecord`/`writeRecordTo`・`userConfigDir` seam、WASM 向け no-op の `writeRecord`
- [x] `MainGame.Update/Draw/Layout` へ `defer crashreport.Guard()`
- [x] `runPlay` 頂上へ設置。`main.go` 最初期に `debug.SetTraceback("all")`
- [x] `SetStateProvider` を起動時に登録し、最上位ステート名を諸元へ含める
- [x] テスト。純関数を並列、`Guard`/`currentState` の seam 差し替えを直列で検証
- [x] 調査。Ebiten v2.10.2 の `runMultiThread` は `loopGame` を専用ゲーム goroutine で回し、desktop は recover しない。ゆえに各コールバックの Guard は必須で、`runPlay` 1箇所への簡素化は不可能
- [~] fd 2 をファイルへ複製して全 goroutine の未捕捉 panic も保存する。捕らえ残るのはレンダースレッド等の稀なエンジン panic のみで、OS 別4ファイルの費用に見合わない。ゲームロジックは Guard が覆うので不採用
