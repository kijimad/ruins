---
status: done
tags: []
auto: needs-decision
---

# 本システム

## 背景

知識を得る手段として「本」を追加する。現在はスキル経験値を毎ターン獲得するスキル本のみ実装している。

本でしか伸ばせないスキルもあり（探索、暗視、各種耐性など）、戦闘以外のスキル成長手段として重要な位置づけになる。本なしで上げられるスキルは、練習で上げてから本を読むか、より易しい本を探すことになる。

スキル値・読書進捗はラン内で完結する。毎ランゼロから成長するため、本は1ラン内で読了できる分量に設計する。基本的な本は約20ターンで読了でき、上級の本でも40ターン程度を上限とする。

要件:
- 読み通すには複数ターンが必要で、工数は本によって異なる
- スキル経験値を毎ターン獲得する（レベル適性あり）
- スキル本にはレベル範囲があり、RequiredLevel未満では読めず、MaxLevelとの差で効率が変わる
- 対応する能力値が高いほど1ターンあたりの読書工数が多い
- 危険な状態（近くに敵）になると読書は自動中断される
- プレイヤーが任意のタイミングで読書を手動中断できる
- 中断しても読書進捗は保持され、次回は続きから再開できる
- 読書中は空腹が進行する
- 読了した本は消費される
- インベントリから「読む」アクションで読書を開始できる
- 店・インベントリのspec表示に本のステータスを表示する

## 設計

### Book コンポーネント (`internal/components/book.go`)

```go
type Book struct {
    Effort Pool             // Max=読了に必要な総工数, Current=蓄積した工数
    Skill  *SkillBookEffect // スキル経験値を毎ターン獲得する効果
}

type SkillBookEffect struct {
    TargetSkill   SkillID // 経験値を獲得するスキル
    MaxLevel      int     // この本で上げられるスキル上限
    RequiredLevel int     // 読むのに必要なスキルレベル。0なら誰でも読める
}
```

- `Effort`: 既存の `Pool` 型を再利用。Max=総工数（雑誌200、専門書300、教本400）、Current=中断しても保持される蓄積工数
- `Skill`: ポインタフィールドで効果の有無を表現する

EntitySpec, Components に `Book` を追加する。`save:"true"` で永続化する。

### 工数と読書速度

1ターンあたりの読書工数は `基本工数10 + 対応する能力値` で計算する。能力値が高いほど速く読める。

```go
func calcEffortPerTurn(book, actor, world) int {
    baseEffort := 10
    abilityValue := SkillAbility[book.Skill.TargetSkill] の能力値
    return baseEffort + abilityValue
}
```

### 経験値効率

プレイヤーのスキルレベルと本のMaxLevelの差に基づく。

```
diff = MaxLevel - playerLevel（正=本が難しい）

diff > 5  → 0%   難しすぎて理解できない
diff = 5  → 50%
diff = 0  → 100% 最適
diff = -5 → 10%  易しすぎて得るものがない
diff < -5 → 10%

難しい側: efficiency = 100 - diff*10
易しい側: efficiency = 100 + diff*18
```

既存の `skill.GainExp` を再利用し、`GrowthConfig.BaseExp` を efficiency で調整する。

### ReadActivity (`internal/activity/read.go`)

RestActivityと同じパターンの多ターンアクティビティ。

```
Info:
  Name:          "読書"
  Interruptible: true
  Resumable:     true

Validate:
  - comp.Targetが設定されていること
  - 対象がBookコンポーネントを持つこと
  - 本が読了済みでないこと
  - RequiredLevel以上のスキルレベルがあること
  - 周囲に敵がいないこと（isAreaSafe共通関数）

DoTurn:
  1. 安全性チェック → 敵がいれば自動中断
  2. Effort.Current += calcEffortPerTurn()
  3. progressHunger() で空腹進行
  4. applyPerTurnEffect: 経験値効率を計算して GainExp を呼ぶ
  5. スキルアップしたら CharModifiers 再計算
  6. Effort.Current >= Effort.Max なら Complete

Finish:
  - 読了メッセージ
  - worldhelper.ChangeItemCount で本を消費

Canceled:
  - 中断メッセージ。Effort.Currentはそのまま保持
```

### 共通ユーティリティ (`internal/activity/activity.go`)

ReadActivityとRestActivityで共有する関数:
- `isAreaSafe`: 周囲3x3に敵がいないかチェック
- `progressHunger`: ターン経過による空腹進行（MoveActivityでも使用）

### spec表示 (`internal/widgets/views/spec.go`)

店・インベントリのアイテム詳細に本の情報を表示する:
- ヘッダー: 「本」（読了済みなら「本（読了）」）
- スキル: 対象スキル名
- Lv: `RequiredLevel → MaxLevel`（Nerd Fonts矢印アイコン使用）
- 進捗: パーセンテージ表示（0%の場合は非表示）

### インベントリ連携 (`internal/states/inventory_menu.go`)

- `getActionItems()`: Bookコンポーネントを持つアイテムに「読む」アクションを表示
- `executeActionItem()`: 残り工数（Effort.Max - Effort.Current）をDurationとしてReadActivityを実行

### TOML データ定義

```go
type BookRaw struct {
    TotalEffort int
    Skill       *SkillBookRaw
}

type SkillBookRaw struct {
    TargetSkill   string
    MaxLevel      int
    RequiredLevel int
}
```

```toml
[[Items]]
  Name = "マシンガンマガジン vol.43"
  Description = "自動火器の運用について詳しく解説した雑誌。読みやすい"
  SpriteKey = "field_item"
  SpriteSheetName = "field"
  Value = 50
  Weight = 0.3

  [Items.Book]
    TotalEffort = 200
    [Items.Book.Skill]
      TargetSkill   = "rifle"
      MaxLevel      = 2
      RequiredLevel = 0

[[Items]]
  Name = "陸軍射撃マニュアル"
  Description = "軍が発行した射撃技術の教本。体系的で内容が濃い"
  SpriteKey = "field_item"
  SpriteSheetName = "field"
  Value = 200
  Weight = 0.8

  [Items.Book]
    TotalEffort = 400
    [Items.Book.Skill]
      TargetSkill   = "rifle"
      MaxLevel      = 5
      RequiredLevel = 2

[[Items]]
  Name = "高みのサバイバルライフ"
  Description = "極限環境での生存術を記した書籍"
  SpriteKey = "field_item"
  SpriteSheetName = "field"
  Value = 150
  Weight = 0.5

  [Items.Book]
    TotalEffort = 300
    [Items.Book.Skill]
      TargetSkill   = "cold_resist"
      MaxLevel      = 3
      RequiredLevel = 0
```

### 変更ファイル一覧

| ファイル | 変更内容 |
|---|---|
| `internal/components/book.go` | Book コンポーネント、SkillBookEffect、ReadingEfficiency |
| `internal/components/book_test.go` | テスト |
| `internal/components/components.go` | EntitySpec, Components に Book 追加 |
| `internal/components/activity.go` | BehaviorRead 追加 |
| `internal/activity/read.go` | ReadActivity |
| `internal/activity/read_test.go` | テスト |
| `internal/activity/activity.go` | behaviors登録、isAreaSafe、progressHunger |
| `internal/activity/rest.go` | isSafe → isAreaSafe に移行 |
| `internal/activity/move.go` | progressHunger に移行 |
| `internal/raw/raw.go` | BookRaw/SkillBookRaw 追加、newBookFromRaw |
| `internal/states/inventory_menu.go` | 「読む」アクション追加 |
| `internal/widgets/views/spec.go` | addBookInfo 追加 |
| `internal/consts/icons.go` | IconKg 追加 |
| `internal/worldhelper/shop.go` | デバッグ用に本を店売りに追加 |
| `assets/metadata/entities/raw/raw.toml` | 本アイテムデータ追加 |

## コメント

### 採用した方法
- **ポインタフィールドで効果を表現**: `Skill *SkillBookEffect` のnil/非nilで効果の有無を判定する。deadcodeツールに検出されず、型安全
- **工数ベースの読書進捗**: ターン数固定ではなく、能力値で読書速度が変わる。能力値を上げるモチベーションになる
- **Bookを独立コンポーネントにする**: Item+Bookの組み合わせで表現。読了進捗がエンティティごとに保持される
- **RestActivityのパターンを踏襲**: 多ターン、中断可能、再開可能。isAreaSafeとprogressHungerを共通関数に抽出
- **GainExpを再利用**: 既存のスキル成長計算式のBaseExpをefficiencyで調整するだけ
- **読了時に本を消費**: 無限に経験値を得られる悪用を防ぐ
- **読書中の空腹進行**: 安全な場所でのノーリスク読書を防ぐ
- **ラン内完結モデルとの整合**: 毎ランリセットを前提に、基本的な本が約20ターンで読了できるよう工数を200〜400に設定

### 採用しなかった方法
- **Consumableとして実装**: 本は読了まで繰り返し使い、読了時に消費される。Consumableの1回使用とは異なる
- **専用のスキル成長計算式**: GainExpの仕組みで十分。BaseExpを倍率調整するだけで要件を満たせる
- **効果をフラットなフィールドで表現**: SkillIDとMaxLevelを全部Bookの直接フィールドにする方法。効果の組み合わせルールが型で表現できない
- **BookEffectインターフェース（マーカーメソッド方式）**: deadcodeツールが未使用と検出するため不採用
- **`any` 型で効果を表現**: 型安全性が低く、実行時型アサーションが必要になる
- **Type文字列による種類判定**: TOMLの構造と対応が不明瞭。ネスト構造のほうが明確
- **即時適用 + ペナルティ方式**: 本という形式の楽しさ（タイトル、概要、進捗）が失われる
- **ターン数固定の読書速度**: 能力値が活かせない。工数ベースにすることで成長の実感を得られる

## 進捗

### 要件

- [x] 読み通すには複数ターンが必要。ReadActivity は Duration を持つ多ターンアクティビティ `internal/activity/read.go`
- [x] スキル経験値を毎ターン獲得。`applyPerTurnEffect` が `skill.GainExpScaled` を呼ぶ `internal/activity/read.go:181`
- [x] RequiredLevel未満では読めず、MaxLevel差で効率変化。`Book.CanRead` と `ReadingEfficiency` `internal/components/book.go:21,50`
- [x] 対応能力値が高いほど工数増。`calcEffortPerTurn` = 基本10 + `getSkillAbilityValue` `internal/activity/read.go:216,225`
- [x] 近くに敵で自動中断。DoTurn 冒頭の `isAreaSafe` 判定で `Cancel` `internal/activity/read.go:107`
- [x] 手動中断可能。Info の `Interruptible: true` `internal/activity/read.go:30`
- [x] 中断しても進捗保持。`Effort.Current` を保持し Canceled で消さない `internal/activity/read.go:167`
- [x] 読書中の空腹進行。`progressHunger` 呼び出し `internal/activity/read.go:125`
- [x] 読了した本は消費。Finish で `lifecycle.ChangeItemCount(-1)` `internal/activity/read.go:157`
- [x] インベントリから「読む」で開始。`actionRead` と `ReadActivity` 実行 `internal/states/inventory_menu.go:457,576`
- [x] spec表示に本ステータス。`addBookInfo` `internal/widgets/views/spec.go:224`

### 設計・成果物

- [x] Book コンポーネント。Effort/Skill/SkillBookEffect/ReadingEfficiency。型は `Pool` でなく `IntPool` を使用 `internal/components/book.go`
- [x] Book テスト `internal/components/book_test.go`
- [x] EntitySpec, Components に Book 追加。genspec 登録表 + 生成物 `internal/components/genspec/registry.go:83`, `internal/components/components_gen.go:67`
- [x] BehaviorRead 追加 `internal/components/activity.go:56`
- [x] ReadActivity `internal/activity/read.go`
- [x] ReadActivity テスト `internal/activity/read_test.go`
- [x] behaviors登録・isAreaSafe・progressHunger `internal/activity/activity.go:30,182,206`
- [x] rest.go を isAreaSafe に移行。旧 isSafe は残存せず `internal/activity/rest.go:57,78`
- [x] move.go を progressHunger に移行 `internal/activity/move.go:222`
- [x] TOML から Book をパース。設計の BookRaw/SkillBookRaw/newBookFromRaw は、OpenAPI 生成型 `oapi.Book` と `newBookFromAPI` として実装 `internal/raw/raw.go:184,315`
- [x] 「読む」アクション追加 `internal/states/inventory_menu.go`
- [x] addBookInfo 追加。ヘッダー・スキル・Lv矢印・進捗% を表示 `internal/widgets/views/spec.go:224`
- [x] IconKg 追加 `internal/consts/icons.go:32`
- [x] デバッグ用に本を店売りに追加。ファイルは `internal/world/gameaction/shop.go:95` に移動済み
- [x] 本アイテムデータ3種を追加。マシンガンマガジン vol.43・陸軍射撃マニュアル・高みのサバイバルライフ `assets/metadata/entities/raw/raw.toml:6162,6690,6754`

### 補足

- spec のヘッダーは常に「本」で、設計の「本（読了）」表記は未実装。読了状態はヘッダーに反映されない `internal/widgets/views/spec.go:230`
- 本消費は設計の `worldhelper.ChangeItemCount` でなく `lifecycle.ChangeItemCount` を使用。パッケージ移設によるもので機能は同等
