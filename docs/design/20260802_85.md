---
status: done
tags: [ui, refactor]
auto: needs-decision
---

# メニュー画面を宣言的 MenuScreen コンポーネントへ統一する

## 背景

doc 84 でメニューをタブ化し、`newTabScreenUI` でモーダルの外枠を共通化した。しかし共通化できたのは
「見た目の枠」と「カーソル核ロジック `hooks.UseTabMenu`」だけで、両者を繋ぐ配線は各 state に手書きのまま
残った。結果、以下2つの痛みが生じている。

1. 使いにくい。同じキーが画面ごとに違う挙動をする。
2. 一貫性を保てない。同じ処理が各 state にコピーされ、追加や修正のたびに食い違う。

1つのメニュー画面は実際には次の掛け算で成り立つ。

**データ模型 × 入力 × 選択・ページング状態 × 描画 × サブ窓/詳細窓 × アクション実行**

このうち共通化済みは「枠の描画」と「カーソル核」のみ。残りのグルーが各 state に散り、`character_state.go`
は765行、`craft_menu.go` は616行に膨らんだ。

### 現状の不統一（コード調査で確定した事実）

振る舞いの分岐。これが「使いにくい」の正体で、共通関数では治せない。

| 症状 | 実態 |
|---|---|
| ←→ の意味が画面で違う | タブ切替 / ページ送り / 無効 の3通り |
| 上下の連射が画面で違う | メイン一覧はリピート有、サブ窓はリピート無 |
| キー説明が出たり出なかったり | フッター有 / help 行自作 / 無し が混在 |
| `[ ]` メンバー切替が発見不能 | character だけ `Update` 内で生キー直読み、案内なし |
| 1画面の件数が違う | 18 と 20 が混在 |
| 入力の入口が2系統 | インターフェース `HandleInput` 組と生 ebiten 直読み組に分裂 |

重複コード。共通関数化で消せる。

- アクションウィンドウ生成 5コピー
- ウィンドウ用カーソル reducer 6コピー
- 詳細モーダル生成 2コピー
- 「候補＋閉じる」型 5コピー
- 選択→ウィンドウ遷移の境界チェック 5コピー
- 所持金行 完全一致2コピー
- itemCounts ループ→`UseTabMenu` 全 state
- ページインジケータの空文字埋め 3コピー
- 3x3グリッドの空セル骨組み shop/craft/storage

二重抽象。`styled.NewTable*` 手組み系と、settings のみが使う `tabmenu.View` 系が併存し、後者の増分更新
API `UpdateFocus` は死蔵している。`UseRef`/`UseTimer` もメニューでは未使用。

## 要件

- 全メニューでキー操作の意味を統一する。ユーザーがどの画面でも同じキーで同じ操作ができる。
- キー案内を全画面で常設し、操作を発見可能にする。
- メニュー画面のループを単一のコンポーネントが所有し、各 state は宣言だけを書く。振る舞いの分岐が
  再発しない構造にする。
- 既存の hooks と styled 資産を活かす。丸ごと再描画方式は当面維持する。
- golden テストで各画面の見た目を守りながら、1メニューずつ段階移行する。

## 設計

### 正準キーマップ

`menuscreen` コンポーネントが唯一の入力入口となり、全メニュー共通のキーマップを持つ。生 ebiten 直読みと
`HandleWindowInput` のリピート差を撤廃する。

| キー | 動作 |
|---|---|
| ↑ ↓ | カーソル移動。リピート有で統一 |
| ← → | タブ切替に固定 |
| Enter | 決定 |
| x | 詳細表示 |
| Esc | 戻る / 閉じる |
| `[` `]` | 対象切替。member 切替など対象がある画面のみ |

ページングはカーソル位置から自動導出する。`UseTabMenu` は `ItemsPerPage` からページを導出できるため、
カーソルが端を越えたら自動でページを繰る。これにより ←→ をページ送りに使う必要が消え、意味の3分裂が
構造的に根絶される。

### 共通化の粒度

当初は全画面を単一の宣言的 `Spec` で置換する案を検討したが、実コードを精読した結果、storage の
dual-list や craft の結果窓など画面固有の制御が多く、単一の Spec は漏れやすい巨大抽象になると判断した。
段階1で振る舞いは既に統一済みなので、段階2以降は各 state の制御フローを保ったまま、真に重複する
機械部分だけを共通化する。これにより出力が不変になり golden を更新せず移行できる。詳細は
「採用しなかった方法」を参照する。

`internal/widgets/menuscreen` が提供する共通部品。描画とカーソルロジックに責務を限り、どの操作を
実行するかや対象の解決はドメイン判断として呼び出し側の state が持つ。

```go
// WindowCursorReducer は選択肢ウィンドウの上下カーソル reducer を返す。端で循環する
func WindowCursorReducer(count int) func(int, inputmapper.ActionID) int

// BuildActionWindow は選択肢を縦に並べるサブウィンドウを rect の位置へ組み立てる
func BuildActionWindow(world w.World, rect image.Rectangle, title string, actions []string, selectedIndex int) *widget.Window

// BuildDetailWindow はアイテムや装備の詳細ウィンドウを rect の位置へ組み立てる
func BuildDetailWindow(world w.World, rect image.Rectangle, name, desc string, entity ecs.Entity) *widget.Window
```

`internal/states/lib.go` が提供するメニュー共通のヘルパ。

- `menuNavHint(hasTabs bool, extras ...string) string`: フッターのキー案内。全画面で常設する
- `newCurrencyRow(currency, res)`: 所持金行。商店と酒場で共通
- `newPageIndicator(pg, res)`: ページ表示行。空でも高さを保つ
- `newThreeColContent(topRight, midLeft, midRight, bottomLeft)`: 3列本文の骨組み。商店・合成・収納で共通
- `menuItemsPerPage`: 1ページ件数の既定。キャラクター情報タブのみ行数が変わるため別に持つ

これらにより、アクション窓5コピー・カーソル reducer 6コピー・詳細窓2コピー・所持金行2コピー・
ページ表示4コピー・3列骨組み3コピーが、それぞれ1つに集約される。

### 段階移行

各段階で `make check` を通し、golden を更新する。1メニューずつ移す。

1. 正準キーマップと入力契約の統一を先行する。振る舞いの分裂を先に治す。単独で出荷できる。
2. `internal/widgets/menuscreen` を新設し、最も複雑な item_action と character を移行して API を検証する。
3. shop / craft / storage / tavern / squad を移行する。3x3グリッド骨組みと重複を吸収する。
4. main / settings を枠へ回収する。`tabmenu.View` 系の二重抽象を撤去する。

## 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/widgets/menuscreen/doc.go` | パッケージ説明。責務と使い分けを記載する |
| `internal/widgets/menuscreen/window.go` | `WindowCursorReducer` / `BuildActionWindow` / `BuildDetailWindow` |
| `internal/states/lib.go` | 正準キーマップ・通貨行・ページ表示・3列骨組み・1ページ件数の共通ヘルパ |
| `internal/states/window.go` | 左右をタブ切替へ固定、上下移動のリピート差を解消 |
| `internal/inputmapper/action.go` | `ActionMenuSubjectPrev` / `ActionMenuSubjectNext` を追加 |
| `internal/states/item_action_state.go` | 詳細窓を BuildDetailWindow へ、左右→タブ読み替えを撤去 |
| `internal/states/character_state.go` | 対象切替を契約へ回収、アクション窓・詳細窓・reducer を menuscreen へ、命令タブを追加 |
| `internal/states/shop_menu.go` | アクション窓・reducer・通貨行・ページ表示・3列骨組みを共通化 |
| `internal/states/craft_menu.go` | アクション窓・reducer・ページ表示・3列骨組みを共通化 |
| `internal/states/storage_menu.go` | 自作ヘルプ撤去しフッター常設、ページ表示・3列骨組みを共通化 |
| `internal/states/tavern_menu.go` | アクション窓・reducer・通貨行を共通化、フッター常設 |
| `internal/states/squad_menu.go` | アクション窓・reducer を共通化、フッター常設 |
| `internal/states/component_debug_state.go` | ページ表示・1ページ件数を共通化 |
| `internal/states/settings_menu.go` | `tabmenu.View` 依存を除去しテーブル描画へ、フッター常設 |
| `internal/states/golden_test.go` | フッター追加と設定移行に合わせ golden を再生成 |

## 採用しなかった方法

### 全画面を単一の宣言的 Spec で置換する

各 state を Spec 宣言だけに縮める案。魅力的だが、storage の dual-list や craft の結果窓など画面固有の
制御が多く、単一の Spec はそれらを吸収するため肥大化し漏れやすい巨大抽象になる。振る舞いの統一は
段階1で入力契約側に寄せて達成済みなので、Spec でループごと奪う必要はない。よって部品抽出に切り替えた。

### 共有ヘルパ抽出のみで振る舞いは触らない

重複だけを関数化し入力は各 state のままにする案。重複は消えるが左右キーの意味の分裂などの
使いにくさが残る。本設計は段階1で振る舞いを先に統一し、その上で段階2以降に部品を抽出することで、
使いにくさと重複の両方を治す。

### ebitenui 標準 List へ全面移行

カーソル・スクロール・フォーカスを ebitenui に委譲し、丸ごと再描画をやめる案。真の複雑さの根まで消せるが、
hooks + rebuild というこのリポジトリの土台思想ごと置換になり blast radius が大きすぎる。golden も全変更。
今の痛みに対して過剰。

### 独自 immediate-mode GUI

ebitenui と styled を捨て自作する案。コストが極大で、現在の資産をすべて捨てることになる。正当化できない。

## コメント

- 案1を段階導入する。既存資産を活かしつつ、使いにくさ（振る舞いの分裂）と保守性（重複）の両方を治せる。
- 段階1のキーマップ統一が最も高価値で低リスク。単独で出荷して効果を確認してから後続へ進む。

## 進捗

- [x] 段階1: 正準キーマップと入力契約を統一する
  - [x] ←→ の意味をタブ切替に固定し、ページングをカーソルへ従属させる
  - [x] 上下移動のリピート有無を統一する
  - [x] フッターのキー案内を全画面常設する
  - [x] character の対象切替を handleInput→DoAction の契約へ回収する。切替キーは配列非依存の , . を使う
  - [~] item_action の動詞キーは handleInput 内でアクションを返す正規の読みのため回収不要。さらなる共通化は段階2の menuscreen へ委ねる
- [x] 段階2: `internal/widgets/menuscreen` を新設し重複部品を集約する
  - [x] WindowCursorReducer / BuildActionWindow / BuildDetailWindow を実装する
  - [x] item_action と character の詳細窓・アクション窓・reducer を移行する
  - [~] 全画面を単一 Spec で置換する案は巨大抽象になるため採らず、部品抽出に切り替えた
- [x] 段階3: shop / craft / storage / tavern / squad を移行する
  - [x] アクション窓・reducer を menuscreen へ集約する
  - [x] 通貨行・ページ表示・3列骨組みを lib.go の共通ヘルパへ集約する
  - [x] `itemsPerPage` 既定を1定数へ集約する。キャラクター情報タブのみ 18 を残す
- [x] 段階4: 設定画面を標準テーブル描画へ揃える
  - [x] `tabmenu.View` 依存を除去する。tabmenu パッケージはメッセージ選択肢で使うため残す
  - [~] main は全画面のタイトル画面でフィールド上のモーダルではないため、標準枠へは回収せず現状を保つ
