# シームレスワールド Phase 8: 状態統合・ポータルの行き先保持・オーバーマップ中心化

## 背景

[20260719_63.md](./20260719_63.md) で共存方式のステージ機構を実装し、ダンジョンの階層往復が動く。全ステージのエンティティを同一 world に共存させ、`StageBound` で所属を、`Suspended` で稼働可否を表し、往復を `stage.SwapTo` に集約した。63.md は「遺跡は『隣のステージがオーバーワールド帯』という特殊ケースにすぎない」「往復はマーカー付け替えに還元」と述べた。

本 Phase はその論理を最後まで進め、**世界をオーバーマップ中心に一本化**する。到達したい姿:

- 状態は1つ。オーバーワールドも遺跡も同じ探索状態で扱う。
- ポータル、すなわち階段・入口が行き先を保持し、往復は「触れて行き先へ swapTo」に還元される。
- 街はオーバーマップ上に生成され、新規ゲームの開始点になる。
- 「戻れない通常ダンジョン」と、その起動口であるダンジョン選択・街からの起動を廃止する。`StageKindDungeon` を撤廃する。

これは [[project-stagekind-dungeon-transitional]] に記録した「StageKindDungeon は過渡的、将来オーバーワールド+遺跡へ寄せて撤廃」の実行である。

## 前提の再確認（設計が依拠する事実）

- **`OverworldState` は既に `DungeonState` を埋め込む薄いラッパー**。`OverworldState.Update` は `DungeonState.Update` へ委譲し、末尾で `updateFront` と `maybeShift`（帯シフト）を足すだけ。描画も DungeonState へ委譲。よって統合は「この2フックを DungeonState 側へ畳む」だけで済む。
- **共存方式の忠実復元はエンティティが serde されるから**であり、**State マシンのスタックは serde 対象でない**。ゆえにステージの継続情報はスタックに預けてはならない。ECS 側、コンポーネントとシングルトンに置く。前設計案の TransPush/Pop 退避はここで reload に破綻したため棄却する。
- **`findPortalPosition` は ActiveFilter で Suspended を除外**する。退避中ステージのポータルは検索に出ない。帰還は「入口を探す」でなく「行き先をポータルが保持」で解く。

## 要件

- 状態を統合する。オーバーワールドと遺跡フロアを単一の探索状態が扱う。挙動は現在ステージの種別で分岐する。
- ポータルは行き先 `StageKey` と行き先座標を保持する。触れると `stage.SwapTo(行き先)` し、行き先座標へプレイヤーを配置する。入口・上り階段・下り階段のすべてがこの1機構に還元される。
- 遺跡はオーバーワールドの入口から入り、上り階段でオーバーワールドへ戻る。戻り先座標は入口の座標で、入場時に遺跡の上り階段へ焼き込む。
- 街はオーバーマップの開始チャンクに生成し、新規ゲームはそこで始まる。
- `StageKindDungeon`・ダンジョン選択・街からのダンジョン起動を廃止する。ステージ種別は Overworld と Ruin の2つにする。
- すべてのステージは離れても保持され、徒歩で到達し直せる。セーブ・ロードで共存状態が復元される。

## 設計

### 1. 状態統合 — DungeonState に一本化

`OverworldState` を廃し、`DungeonState` に統合する。`updateFront`・`maybeShift`・帯生成・`restoreFromSave` を DungeonState へ移し、`CurrentStage.Kind == Overworld` のときだけ実行する。

```go
func (st *DungeonState) Update(world w.World) (...) {
    // 既存の探索更新（入力・ターン・視界・カメラ・HUD・死亡判定）
    ...
    if query.GetDungeon(world).CurrentStage.Kind == gc.StageKindOverworld {
        st.updateFront(world)
        st.maybeShift(world)
    }
}
```

帯ドライバ、ChunkGen と Band は DungeonState のフィールドとして持つ。enter/exit は同一 State 内の `swapTo`（TransNone）なので State は破棄されず、遺跡滞在中もドライバはメモリに残る。退避中の帯はシフトしないので、戻ったとき ECS とドライバが一致する。reload 時は `restoreFromSave` が SeamlessBand から再構築する。State 名は責務が広がるため将来 `FieldState` 等へ改名を検討するが、本 Phase では `DungeonState` のままにして差分を絞る。

### 2. ポータルの行き先保持 — 往復の一般化

ポータルを「行き先を持つステージ・グラフの辺」に一般化する。ポータルのエンティティに行き先を持たせる。

| ポータル | 行き先(StageKey) | 行き先座標 |
|---|---|---|
| オーバーワールドの遺跡入口 | 遺跡1階 | 遺跡の入口 |
| 遺跡の上り階段(最上階) | オーバーワールド | 入った入口の座標 |
| 下り階段 | 1つ深い階 | その階の入口 |
| 上り階段(中間階) | 1つ浅い階 | その階の下り階段 |

- ポータルに触れる → そのポータルの行き先 `StageKey` へ `stage.SwapTo` → 行き先座標へ `MovePlayerToPosition`。種別分岐なしの単一機構。
- 未訪問の行き先は SwapTo の generate で生成し、生成時に「戻りのポータル」へ戻り先座標を焼き込む。
- 行き先座標はポータルのコンポーネントに持つので serde 対象。reload を跨いで戻り先が保たれる。「どの入口から入ったか」を階段自身が覚える。
- これで `findPortalPosition` の先着ヒューリスティックを廃し、複数入口があっても曖昧さが無い。

### 3. 街をオーバーマップに生成

- 街はステージでなくオーバーワールドの地物。開始チャンク、`eastIndex==0` 付近に街の構造物、店・収納・隊員雇用などの prop を決定的に配置する。
- 新規ゲームは、キャラ作成後に街を含むオーバーワールドを生成し、街の位置でプレイヤーを開始する。`TownState` と、そこへ至る導線を廃止する。街の機能は overworld 上の prop への相互作用で提供する。

### 4. 破棄スコープ

一方向ダンジョンと街への完全離脱が無くなるため、「潜行を丸ごと捨てる」完全離脱が消える。全ステージは共存・徒歩往復可能で保持される。`DungeonState.OnStop` の全フィールド一括 purge を廃止する。world を捨てるのはタイトルへ戻る・ロードのときで、そこは world リセットが担う。ステージ単位の破棄が要る場合は `stage.Purge` をハンドラで明示的に呼ぶ。

### 5. StageBound タグ付け（前提）

- チャンク生成直後に `stage.Bind(world, gc.NewOverworldStage())` で帯エンティティを束縛する。
- `CurrentStage` は開始時に `NewOverworldStage()`。以降は swapTo が更新する。

## 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/states/dungeon.go` | `updateFront`/`maybeShift`/帯生成/`restoreFromSave` を統合。Overworld 種別で分岐。`OnStop` の一括 purge 廃止。ポータル触知を行き先ベースへ |
| `internal/states/overworld.go` | 廃止。中身を dungeon.go へ移設 |
| `internal/overworld/chunkgen.go` | 生成直後に `stage.Bind(NewOverworldStage())`。開始チャンクに街を配置 |
| `internal/world/stage/stage.go` | 必要なら `PurgeKind` 等の補助 |
| `internal/components/` | ポータルの行き先 `StageKey`+座標を持つコンポーネント。遺跡入口の種別 |
| `internal/activity/execute_interaction.go` | ポータル触知を行き先ベースの単一ハンドラへ |
| `internal/raw/`・`assets/` | 遺跡入口 prop。街の prop 群 |
| ダンジョン選択・街起動・`StageKindDungeon` 関連 | 廃止。`dungeonStageKey`→遺跡/オーバーワールドの生成へ再編 |
| `internal/states/factories.go` | `NewTownState`・ダンジョン選択の除去。新規ゲーム開始をオーバーワールド+街へ |
| 各 `_test.go`・golden | 統合後の状態・往復・serde・開始点のテスト更新 |

## 採用しなかった方法

- **OverworldState を残し TransPush/Pop で遺跡へ出入り**: 前案。State スタックが serde 外なので、遺跡中セーブ→ロードで戻り先 State が消え TransPop が破綻する。共存方式の「継続は ECS に置く」原則に反するため棄却。
- **専用 RuinState を新設**: 遺跡専用状態。DungeonState と描画・往復・入力をまるごと重複させる。遺跡はダンジョンと構造が同じで、統合 State + ポータル行き先で足りるため不要。
- **帰還を入口 prop 探索で解く**: `findPortalPosition` で入口を探して戻る。入口が複数あると先着で誤る。ポータルが行き先座標を持つほうが曖昧さゼロで serde 安全。
- **街を専用ステージにする**: 街を Ruin と同様の別ステージにして入口から入る。だが「開始点」なので入場でなく初期在場が要り、オーバーマップ地物とするほうが素直。

## コメント

- 統合は `OverworldState` が既に `DungeonState` ラッパーなので小さい。まず挙動保存の統合から入り、その後にポータル一般化・遺跡・街生成・旧ダンジョン撤廃を重ねる。
- 撤廃(旧ダンジョン・選択・街起動)は代替(遺跡・街オーバーマップ化)が入ってから最後に行い、常にゲームが動く状態を保つ。
- 新たな不変条件: Update は `CurrentStage.Kind` で分岐する。swapTo が CurrentStage を更新するので、遷移中に古い種別で誤動作しないようテストで固める。

## 進捗

- [x] Phase A: 帯の `StageBound` タグ付け + `CurrentStage=overworld`。単体テスト
- [x] Phase B: `OverworldState` を `DungeonState` へ統合（挙動保存）。golden/テスト維持
- [x] Phase C: ポータルの行き先(StageKey+座標)保持。ascend を Connection ベースへ、descend は生成時に上り階段へ結線。既存往復テスト維持
- [x] Phase D: `InteractionRuinEnter` + `RuinEntrance` + enterRuin/exitRuin をポータル行き先で実現。enter/exit の往復テスト・相互作用配線テスト
- [x] Phase E: 遺跡入口をオーバーワールド開始チャンクに配置。街を `spawnTown` でオーバーマップに生成し新規ゲーム開始点をそこへ。店・雇用・合成・収納を prop 相互作用へ移行
- [x] Phase F: 旧ダンジョン選択・街ハブ系を一括撤廃。`OnStop` 一括 purge 廃止。`StageKindDungeon` は既に撤去済み。全クリア発火はオーバーワールド滞在時へ移設
- [x] Phase G: serde+共存の統合テストを追加。実生成した街+遺跡の共存状態をセーブ→ロード→帰還まで通す。多層遺跡のステージキー不整合バグを検出・修正。起動スモーク済み。実機の通しプレイ検証は手元で行う
- [x] Phase H: シングルトン `Dungeon` の分解。詳細は「追記(2026-07-21)」節。完了により `Dungeon` は identity `CurrentStage` 1フィールドまで痩せた
  - [x] H0: オーバーワールド固有の振る舞い(frost/temperature/move/帯シフト)を、現ステージが前線/帯データを持つかで gate する。型・深度・名前での場所判定を廃す。**interim** で `query.IsOnOverworld` へ集約し漏れ(実バグ)を修正、回帰テスト追加。**恒久形完了**: H3a で `IsOnOverworld` を現ステージのメタが帯データ `SeamlessBand` を持つかのデータ保有判定に置換
  - [x] H1: シングルトン `Dungeon.Depth` を削除し全参照を `CurrentStage.Depth` へ統一。書き込み4箇所は CurrentStage 設定と冗長なので削除。旧セーブは互換
  - [x] H2: 無関係な状態を本来の持ち主へ追い出す。`MinimapSettings`(不変につき consts 化)・`GameTime`(グローバル時間シングルトン)・`SelectedWeaponSlot`(`WeaponSelection` シングルトン)・視界キャッシュ(`VisibleTiles`/`LightSourceCache`/`NeedsForceUpdate` → `VisionState` シングルトン、serde 除外)を各々移設
  - [x] H3: `Level`・`ExploredTiles`・帯/前線データをステージメタへ移設し `CurrentStage` で引く。serde・帯シフト・クエリ設計を含む本丸。常に緑を保つため a/b に分割。**H3a**: `StageMeta{Level}` を新設し各ステージへ `StageBound` で束縛。`SeamlessBand` を独立コンポーネント化しオーバーワールドのメタへ付与。`Level` を per-stage 化し、地上帰還時の帯 Level 手動復元ハックを撤去。`IsOnOverworld` を「現ステージのメタが `SeamlessBand` を持つか」のデータ保有判定へ。**H3b**: `ExploredTiles` を `StageMeta` へ移設。serde は json:"-" で除外し `reestablishSingleton` が全 `StageMeta` の探索履歴を空 map で再構築。設計時の `Front *SeamlessBand` フィールド案は、コンポーネント有無で「帯を持つか」を表す方がデータ保有判定の意図に沿い serde も既存形のままなので、独立コンポーネントに変更した
  - [x] H4: `Dungeon.DefinitionName` を削除し `CurrentStage.Name` から導出化。`OverworldStageName == DungeonOverworld.Name` なのでオーバーワールドもダンジョン階も現ステージ名が定義名になり、identity が一本化。地上帰還時の `DefinitionName = ""` クリア、enterDungeon の記録、game.go のデバッグ既定を撤去。生成時は SwapTo が CurrentStage を最後に更新するため、OnStart は `st.DefinitionName` 優先で現ステージ名にフォールバック、descend はクロージャ捕捉の defName を使い順序問題を回避。これで `Dungeon` は `CurrentStage` のみ

## 現状(2026-07-21)

Phase A〜F 完了。世界はオーバーワールド中心に一本化した。旧ダンジョン選択、ダンジョン門
`dungeon_gate`、`NewTownState` と街ハブ、debug の各種ダンジョン開始を撤廃し、ステージ種別は
オーバーワールドと遺跡の実質2つになった。遺跡は入口から入り上り階段でオーバーワールドへ戻る。
全ダンジョンは入口で戻り先を結線するため、`WarpAscend` の結線なしはバグとして error にした。

街レベルデータのうち `DungeonTown`・`PlannerTypeTown` は撤去したが、テンプレート `town_plaza`・
`small_town` とパレット `town.toml` は `PlannerTypeTownPlaza`・`PlannerTypeSmallTown` が共有する
ため残す。全クリア発火は街帰還時からオーバーワールド滞在時へ移した。

`OnStop` の一括 purge は廃止した。world のクリアは MainMenuState.OnStart の全 entity 削除と
save の ECS.Reset が担うため冗長で、共存方式では退避ステージを消しかねず有害だったため。

Phase G で serde+共存の統合テストを2本追加した。実生成した街+遺跡の共存状態をセーブ→別
world へロードし、退避中のオーバーワールド+街と稼働中の遺跡が現物として復元され、上り階段の
結線をたどって地上へ戻れることを、単層と多層の両方で通す。この検証が、多層遺跡を降りて上ると
戻り先が空ステージになる既存のステージキー不整合バグを検出し、`dungeonStageKey` の名前付き
統一で修正した。Phase D の名前付き化が descend まで及んでいなかった取りこぼしで、旧ダンジョン
選択の撤廃で唯一の進入が名前付きになった今に顕在化したもの。

`make test`・`make lint` は全パス。起動スモークでもクラッシュしない。Phase A〜G の設計目標は
達成した。残るは実機での通しプレイ、新規ゲーム→遺跡進入→多層往復→セーブ/ロード→帰還、の
目視確認で、これは手元で行う。

## 追記(2026-07-21): シングルトン Dungeon の分解

### 背景

`components.Dungeon` シングルトンは「冒険1セットの情報」という当初のドメイン概念だが、共存方式で
オーバーワールドと遺跡が同一 world に共存し「1回の潜行」という単位が消えた結果、責務が異なる状態を
詰め込んだ grab bag、実質的な神クラスになっている。現在の11フィールドは次の5系統に分かれる。

- **現ステージの identity**: `CurrentStage`。これが「現在ダンジョンのキー」。
- **identity と重複**: `Depth`(=`CurrentStage.Depth`。dungeon.go:429 で既に `CurrentStage.Depth` を使用)、`DefinitionName`(ほぼ `CurrentStage.Name`。ただし地上帰還で `""` にクリアするズレあり)。
- **現ステージのビュー状態**: `Level`(現ステージ寸法)、`ExploredTiles`(現ステージの探索履歴。進入時リセット)。
- **無関係な状態の誤帰属**: `GameTime`(グローバル時間。turn/temperature/status が参照)、`MinimapSettings`(UI 設定)、`SelectedWeaponSlot`(プレイヤー/戦闘 UI)、`VisibleTiles`/`LightSourceCache`/`NeedsForceUpdate`(VisionSystem の一時状態)。
- **オーバーワールド固有の永続状態**: `SeamlessBand`(帯の serde 対象)。

加えて「オーバーワールドか」の判定方法が乱立している。`CurrentStage == NewOverworldStage()`(名前キー比較)、`CurrentStage.Depth == 0`、`SeamlessBand.Active`、`Front.Active`、`session != nil`、`isSeamless()` が場所判定として互換的に使われるが、これらは実は3つの別概念を混同している。

1. **現ステージがオーバーワールドか**(場所): frost 描画・帯シフトゲート・ascend の戻り先。
2. **この State が帯モードか**(`isSeamless`/`session != nil`): 遺跡滞在中も真。State は破棄されず帯を保持するため。
3. **前線が有効か**(`Front.Active`): 帯の性質。共存方式では遺跡滞在中も真のまま残る。

`Front.Active` を「オーバーワールドにいる」の代理に使う誤りが実害を出している。霜(修正済み)に加え、`temperature.go` の `frostZoneModifier` と `move.go` の `frontAllowsMoveTo` が `Front.Active` だけで判定し、遺跡内で寒さ効果や移動制限が漏れる同型のバグを抱える。名前文字列での直接判定もマジック定数依存で脆い。

### 要件

- 現ダンジョンの identity は `CurrentStage` に集約する。そこから導出できる冗長フィールドは持たない。
- 現ステージの状態はステージ側に置き、`CurrentStage` で引く。共存方式の理念「継続は ECS に置く」に沿わせる。
- ダンジョンと無関係な状態は本来の持ち主へ移す。
- 分解の各段はゲームが常に動く状態を保ち、`make check` で担保する。

### 設計

4つの軸で分解する。

0. **オーバーワールドとダンジョンを単一のステージ概念として扱い、違いは保有データで表す**
   - 本質的にオーバーワールドもダンジョン階も同じ「ステージ」で、分岐する種別を持たない。違いは各ステージが**保有するデータ**だけにする。オーバーワールドのステージメタは帯・前線データ(`SeamlessBand`/`Front`)をコンポーネントとして持ち、ダンジョン階のメタは持たない。これは H3 のステージメタ化と一体で、`Level`・`ExploredTiles` と同じく前線/帯データもステージメタへ移す。
   - **型・深度・名前でオーバーワールドを判定するのをやめる**。オーバーワールド固有の振る舞い(frost・temperature・move・帯シフト)は「**現ステージのメタが前線/帯データを持つか**」で gate する。データの有無が判別を兼ねるので、`Name == OverworldStageName`・`Depth == 0`・`Front.Active`・`session != nil` を場所判定に使う乱立が消える。
   - `Depth == 0` は「なぜ深度0が特別か」が読み取れずわかりにくい。名前比較はマジック定数依存。種別フラグ(`StageKind`/`IsOverworld()`)は「オーバーワールドとダンジョンを1つとして扱う」意図に反し分岐点を増やす。データ保有で表せば、振る舞いは自然と該当データを持つステージにだけ効き、判定の一本化と responsibilities の分離が同時に達成される。
   - 3概念の混同も解ける。「現ステージが前線を持つか」(場所+能力)はメタのデータ有無。「State が帯ドライバを持つか」(実行時の帯セッション)は `session`。「前線が有効か」はデータ側の状態。ロード復元判定(`factories.go` の `SeamlessBand.Active`)も、復元した現ステージのメタが帯データを持つかへ寄せる。
   - **注意: これは H3(ステージメタ化)に依存する**。temperature/move の遺跡漏れは実バグなので、H3 完了を待たず、現ステージがオーバーワールドかの単一ヘルパーで先に gate して漏れを止める interim を許容する。恒久形はデータ保有ベース。

1. **identity を `CurrentStage` に集約する**
   - `Depth` を削除し、全参照を `CurrentStage.Depth` へ統一する。冗長で二重管理のため。
   - `DefinitionName` を `CurrentStage.Name` から導出する。地上帰還で `DefinitionName` を `""` にクリアしていた挙動は、オーバーワールドの `CurrentStage.Name`(=`OverworldStageName`)で自然に表せるので不要になる。

2. **`Level`・`ExploredTiles` をステージ側へ移す(採用: 案2)**
   - 各ステージに「ステージメタ」エンティティを1つ持たせ、`Level` と探索履歴を保持する。`StageBound` で自ステージに束縛し、共存の他エンティティと同様に退避・復元される。
   - 参照は `CurrentStage` のメタエンティティを引く。swap でシングルトンを差し替えるのでなく、現ステージのメタを見る。これで「現ステージ状態」がシングルトンから消える。
   - **帯シフトの考慮**: オーバーワールドの探索履歴は帯シフトでキーが平行移動する(`worldstream/band.go` の `translateTileKeyMap`)。この帯ローカル性はメタエンティティへ移しても保つ。ステージメタ単位でシフト対象を特定できるので、むしろ帯とそれ以外の切り分けが明確になる。
   - **serde**: 探索履歴は `GridElement` struct キーで serde 不可のため、現状どおりロード時に再構築するか、serde 可能な表現(スライス等)へ変える。ステージメタが `StageBound` を持つことで、どのステージの履歴かは復元後も自明になる。

3. **無関係な状態を本来の持ち主へ追い出す**
   - `GameTime` → 独立したグローバル時間シングルトン。潜行と無関係で全編にわたる。
   - `MinimapSettings` → UI 設定(config/resources)。実行時に変わらない静的値。
   - `SelectedWeaponSlot` → プレイヤー/装備状態。
   - `VisibleTiles`/`LightSourceCache`/`NeedsForceUpdate` → VisionSystem 内部、または Vision 専用シングルトン。視界計算の一時状態で、描画が読むだけ。

これらを終えると `Dungeon` は identity(`CurrentStage`)だけまで痩せる。`SeamlessBand`/`Front` は軸0のとおりオーバーワールドのステージメタへ移り、他ステージと同様に serde・退避される。痩せた `Dungeon` は「現在地を指すポインタ」に等しく、名前も責務に合わせて改名を検討する(例: `CurrentLocation` 等)。あるいは単一フィールドなら別の singleton へ畳んで `Dungeon` 自体を無くすことも視野に入る。

### データ定義

分解の前後を struct で示す。

```go
// 現状: grab bag
type Dungeon struct {
	Level              Level                       // → ステージメタ(H3)
	Depth              int                         // 削除(=CurrentStage.Depth) ← H1 済
	CurrentStage       StageKey                    // 残す。identity
	ExploredTiles      map[GridElement]bool        // → ステージメタ(H3)
	MinimapSettings    MinimapSettings             // → consts ← H2 済
	NeedsForceUpdate   bool                        // → Vision(H2)
	DefinitionName     string                      // 導出化(=CurrentStage.Name) ← H4
	GameTime           GameTime                    // → GameTime singleton(H2)
	SelectedWeaponSlot int                         // → WeaponSelection singleton ← H2 済
	VisibleTiles       map[GridElement]bool        // → Vision(H2)
	LightSourceCache   map[GridElement]LightInfo   // → Vision(H2)
	SeamlessBand       SeamlessBand                // → オーバーワールドのステージメタ(H3)
}

// 目標: identity だけ
type Dungeon struct {
	CurrentStage StageKey
}

// 追い出し先
type WeaponSelection struct{ Slot int }           // H2 済。singleton
type GameTimeState  struct{ GameTime GameTime }    // H2。singleton
type VisionState    struct {                       // H2。singleton。全て毎フレーム/視界更新で再構築、serde 除外
	VisibleTiles     map[GridElement]bool
	LightSourceCache map[GridElement]LightInfo
	NeedsForceUpdate bool
}

// ステージメタ: ステージごとに1つ。StageBound で自ステージに束縛され共存・退避・serde される
type StageMeta struct {
	Level         Level
	ExploredTiles map[GridElement]bool  // 帯ローカル。オーバーワールドのみ帯シフトで平行移動
	Front         *SeamlessBand         // オーバーワールドのメタだけ持つ。有無が「前線を持つか」の判別を兼ねる(軸0)
}
```

### 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/components/dungeon.go` | `Depth`・`DefinitionName` 削除。`Level`・`ExploredTiles`・`SeamlessBand`/`Front`・`Minimap`・`SelectedWeaponSlot`・`GameTime`・視界3フィールドを移設。`CurrentStage` だけに痩せる |
| `internal/components/`(新規) | ステージメタ(`Level`・探索履歴・前線/帯データ)・GameTime・Vision・UI 設定の受け皿コンポーネント。登録表 + `make generate` |
| `internal/systems/temperature.go`・`internal/activity/move.go` | 前線効果を現ステージの前線データ有無で gate。遺跡内漏れ(実バグ)を塞ぐ |
| `internal/world/stage/` | ステージメタの生成・`CurrentStage` からの引き当てヘルパー |
| `internal/systems/vision.go` | 視界キャッシュを Vision 側へ。`Level`/`ExploredTiles` の参照をメタ経由へ |
| `internal/systems/hud_extractor.go` | `MinimapSettings`/`SelectedWeaponSlot`/タイル色の参照先を移設先へ |
| `internal/states/dungeon.go` | `Depth` 参照を `CurrentStage.Depth` へ。`Level` 設定をステージメタへ |
| `internal/overworld/session.go`・`worldstream/band.go` | 帯の `Level`・探索履歴の帯シフトをステージメタ経由に |
| `internal/save/serde.go` | 移設後の serde 対象・除外を更新。ステージメタの復元 |
| 各 `_test.go` | 参照先の更新。identity 導出・per-stage 保持・移設の単体テスト |

### 採用しなかった方法

- **案1: シングルトンを現ステージのビューのまま残す**。`Level`・`ExploredTiles` を swap で差し替える今の方式。変更は小さいが、「現ステージ状態」と「グローバル状態」の同居が残り、共存方式の理念から外れる。将来ステージが増えると差し替えコストと取り違えバグの温床になる。今回の一連の帰還バグ(Level 未復元・現ステージ誤判定)も、状態がシングルトンに集約され swap で暗黙に差し替わる構造が一因だった。
- **`GameTime` を `Dungeon` に残す**。時間は潜行と無関係で全編グローバル。誤帰属を続けると「Dungeon が無いと時間が読めない」不自然な依存が残る。
- **種別で判定する(`StageKind` enum / `IsOverworld()` 述語 / `Depth == 0`)**。オーバーワールドかを型・フラグ・深度で分岐する案。いずれも「オーバーワールドとダンジョンを1つとして扱う」意図に反し、分岐点と乱立を残す。`Depth == 0` は0が特別な理由が読めずわかりにくい。`StageKind` の再導入は全生成・serde・比較への波及で侵襲的。データ保有(前線/帯コンポーネントの有無)で表せば型判定自体が不要になる。
- **場所判定を名前文字列のまま各所で行う**。`Name == OverworldStageName` を散らす現状。マジック定数依存で、意味の違う3概念が混ざり、境界でバグる。実際に霜・気温・移動の漏れを生んだ。
- **一括で全フィールドを移す**。段階(H1〜H4)に分けず一度に行うと `make check` が長く赤になり、共存の帰還経路の回帰を切り分けにくい。設計方針どおり常に動く状態を保って進める。

### 段階(Phase H)

リスクの低い順に H0→H4 で進める。各段で `make check` を通し、共存の往復・serde を回帰確認する。

- H0: `IsOverworld()` 一本化と前線効果の gate。temperature/move の潜在バグを塞ぐので先に片付ける価値が高い。
- H1: `Depth` 削除。最小・確実。
- H2: 無関係状態の追い出し。各々独立に移せる。
- H3: `Level`・`ExploredTiles` のステージメタ移設。serde・帯シフト・クエリを含む本丸。
- H4: `DefinitionName` の導出化。
