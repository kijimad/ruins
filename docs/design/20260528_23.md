---
status: in-progress
tags: []
auto: needs-decision
---

# コンテンツ追加とSteamリリース準備

## 背景

ミニマルリリースに向けて、未使用スプライトをすべてゲーム内コンテンツとして活用し、Steamリリースに必要な要素を整理した。

## ミニマルリリース分析

現在の完成度は約85-90%。主な不足要素:

- **サウンド/オーディオシステム**: BGM・SE未実装。ミニマルリリースにおける最大のギャップ
- ストーリー/テキストの最終調整
- チュートリアル/オンボーディング

## Steamリリースに必要なこと（Ebiten）

### フェーズ1: Steamworks準備
- Steamworksアカウント登録（$100のアプリクレジット料）
- ストアページ作成（カプセル画像、スクリーンショット5枚以上、説明文）
- ストアページ公開後、レビュー審査に最低2週間

### フェーズ2: Steamworks SDK統合

EbitenでのSteamworks統合の選択肢:

1. **steamworks（CGo経由）**: `github.com/nicholasgasior/gosf` など。CGo依存
2. **steam_api DLL直接呼び出し**: `syscall.LoadDLL` で steam_api64.dll をロード。CGo不要
3. **サブプロセス方式**: 別プロセスでSteam APIを呼び、パイプ/ソケットで通信。最もポータブル

ミニマルリリースでは実績やオーバーレイは不要なので、DLL直接呼び出しで `SteamAPI_Init` と `SteamAPI_Shutdown` だけ実装すれば十分。

### フェーズ3: ビルドとリリース
- `steamcmd` でデポにビルドをアップロード
- リリースチェックリスト通過後に公開

## 進捗

### ミニマルリリース分析

- [ ] サウンド/オーディオシステム、BGM・SE。`ebiten/v2/audio` や `oto` の import は皆無、`assets/` に音声ファイルもなく完全に未実装。本文が「最大のギャップ」とする箇所
- [ ] ストーリー/テキストの最終調整。完了を示す痕跡を確認できず未確認
- [ ] チュートリアル/オンボーディング。`internal/states/` に該当ステートがなく未実装

### フェーズ1: Steamworks準備

- [ ] Steamworksアカウント登録。外部作業のため確認不能
- [x] ストアページの説明文。`docs/steam/store.md` に英語・日本語の紹介文と特徴リストを整備済み
- [x] カプセル画像。`docs/steam/generated/` に header/small/main/vertical 等のカプセル画像を生成済み。`docs/steam/README.md` に各アセットの規格も記載
- [ ] スクリーンショット5枚以上。`docs/steam/` にスクリーンショット素材を確認できず未確認
- [ ] ストアページ公開とレビュー審査。外部作業のため確認不能

### フェーズ2: Steamworks SDK統合

- [~] DLL直接呼び出しで `SteamAPI_Init`/`SteamAPI_Shutdown` だけ実装する方針。実装は選択肢1に近い `github.com/hajimehoshi/go-steamworks` 採用のため、本文が推した DLL 直接呼び出し方式は見送り
- [x] Steam API初期化。`internal/steam/steam_enabled.go` の `Init` が `steamworks.Init` と `RestartAppIfNecessary` を実行。`internal/cmd/play.go` で `steam.Init()` を呼び出し。タグなし時は `steam_disabled.go` が no-op
- [ ] `SteamAPI_Shutdown` 相当の終了処理。`internal/steam/` に `Shutdown` は存在せず未実装

### フェーズ3: ビルドとリリース

- [x] Steam向けクロスコンパイル。`make build-steam` から `scripts/build_steam.sh` が Linux Sniper と Windows 向けに `-tags steam` でビルド
- [ ] `steamcmd` でのデポアップロード。`.vdf` 定義や steamcmd スクリプトを確認できず未実装
- [ ] リリースチェックリスト通過と公開。確認不能
