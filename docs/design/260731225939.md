---
status: dropped
tags: [worldgen]
auto: needs-decision
---

# 施設内装の生成 —— doc 260725201431.md の未着手バックログ

## 背景

`20260725_70`、施設内装の生成、status done、の実装で着手しなかった未着手 `- [ ]` を切り出した派生 doc。
doc 260725201431.md 本体は追記その1〜その19 を統合した現行設計と、このブランチで達成した実装の記録を表し done とした。本 doc は
その設計から導かれる今後の候補だけを集約し、着手判断の台帳にする。根拠はすべて doc 260725201431.md の設計章と各追記にある。

## 設計

出所の追記番号ごとに未着手項目を並べる。着手する項目を選んだら、規模が大きければ本 doc から個別の設計 doc を
起こし、小さければここで直接 `- [x]` に倒して実装する。分母は未着手の候補数で、doc 260725201431.md の完了状態には影響しない。

## 進捗

効き目ランク Stage・theme・テンプレート由来。

- [ ] 施設ごとの「設計されたもの度」をメトリクスで測る。散布との差を数値で確認する
- [ ] seed 由来 theme レコード(era/faction/condition/motifs)と署名オブジェクト
- [ ] Cogmind 風テンプレート層。reference-char＋隣接解決＋回転鏡像＋door 数 fingerprint

追記その2から。

- [ ] arc consistency 流の伝播/崩壊分離で有界K配置 pass を組む。パリティ格子 `(x+y)%2` で通行保証を安価に得る
- [ ] palette 二相解決と socket 命名ゾーン契約を content スキーマへ入れる。未接続 socket の既定封鎖を定める
- [ ] space syntax 指標(depth/integration)を決定的解析 pass で算出し重み関数の文脈キーに供給する
- [ ] 連続 trait 近似一致・対称サブセット・決定的ジッタ・隣接同型伝播を stuff/placement へ足す

追記その3から。

- [ ] Archetype へ use_area 方向付きクリアランスを追加し、footprint＋use_area の二重判定と circulation ハード制約を配置成立条件にする
- [ ] 施設文法の初期値表を静的データ化する。コンビニ・診療所・オフィス・倉庫・民家・神社の定石と実基準数値、exclusion_zone、sequence/mirror_pair、zone 単位向き整列
- [ ] ルート分配を部屋タイプ→コンテナ種別→テーブルIDの3段間接化で実装し、zone volume オーバーレイと保証スポーン＋クォータの大域パスを前段に置く。略奪3ノブをパラメータ化する
- [ ] ERA 指標6種と TPKLD 二距離を純関数で実装し、固定 seed 列の golden 分布比較を CI に載せる
- [ ] 開発時 LLM 下書きワークフローを試す。施設別 content 定義を記号のみで出させ、スキーマ検証と人手レビューを経て静的データへ焼き込む

追記その4から。CDDA 到達の必須条件の残り。

- [ ] footprint からの決定的屋根合成を描画側に実装する
- [ ] 家具別収納テーブルの item 語彙を日本の40〜80本へ拡張する。押入れ・仏壇・下駄箱・神棚など。基盤の prop 追加・`[props.storage]` 配線・家具別4テーブル分化は doc 260725201431.md その17・その18 で実装済み
- [ ] FeatureSpec/Extra の2層分配と静的 pity を実装する。頻度の桁分離規約を TOML に明文化する
- [ ] snippet を <地名>タグ展開・専用の発見記録 UI・テキスト断片150〜300本の authoring まで広げる。紙片アイテム8種を loot テーブルへ置く基礎実装は doc 260725201431.md その19 で完了済み
- [ ] BuildingPlan の多層3規則を廃ビル類型に限定導入する。footprint 包含・階段室固定・分割線継承

追記その5から。知覚機序。

- [ ] Alexander 15特性のうち規則化容易な群を分割文法・placement へ制約として実装する。分割比 1:2〜1:3、対称スケール上限、void 予約、密度上限
- [ ] Lynch 5要素の検収テストを作る。生成された街の district 識別・landmark 一意性・道の階層を機械検査する
- [ ] 下地フィールドを 1/f 化し β を計測するガードレールテストを足す。輪郭の box-counting D=1.3〜1.5 の検査も
- [ ] 配置の一様乱数をポアソンディスクと空間相関ジッタへ置換する
- [ ] 築年変数を建物に持たせ、タイルパレット・劣化度・間取り型を築年から導出する
- [ ] 年表レコードを一次データとする chronicle 生成を実装する。同名三面刻印・未解決参照の意図的残置・faction 別の食い違う語り
- [ ] faction スタイルシートを実装する。色・記号・梱包様式の決定的割り当て
- [ ] 二段 theme のパリンプセスト上書きを実装する。基底時代＋転用時代
- [ ] 関係トリプルの配置プリミティブを作る。3要素＋関係の決定的配置と「誰が何をしたか読めるか」の受け入れ基準
- [ ] Ship Log 型の知識記録 UI を検討する。断片の関係網自動記録と控えめな確定演出
- [ ] シードを世界の固有名として UI に出す
- [ ] prospect-refuge の可視性検証を部屋・広場生成に足す。入口から全見えの部屋を禁止する

追記その6から。

- [ ] ZoneCorridor を契約に追加し、spine-first / repair / none の3生成パターンを施設類型で切り替える
- [ ] corridor 専用の軽量 content と幅の実基準を定義する。QA として廊下タグの幾何検証を classifyRoom 系に足す

検討メモから。

- [ ] 不動産サイト・居抜き物件の間取りを統計として抽出し、生成の実データ源にする。間取り型の頻度・隣接関係・
  面積帯・築年別の差・敷地形状を表に起こし、RoomSpec と隣接確率表の下書き、TPKLD の模範参照分布に使う。
  Marson & Musse が国勢調査の面積分布を使ったのと同じ正攻法。図面の転載・トレースは避けパターン抽出に留める。
  データ源は公的統計、住宅・土地統計調査や国勢調査を優先し、民間サイトは利用規約で自動収集の可否を確認する。
  スクレイピングでなく手作業の目視集計を既定にする

追記その7から。doc 260801092932.md、屋外の散布、から。

- [x] 密度場の芯を Room 非依存の `ScatterArea` へ切り出し、interior と overworld の双方から呼ぶ。doc 260801092932.md の
  屋外流用の要求で前倒しした。「決定的ジッタを stuff/placement へ足す」の placement セレクタ統一へ向けた最初の
  一歩で、戸口も壁も前提にしない点セレクタ API の芯ができた。候補の可否は accept 述語へ委ね、dirt 判定や占有判定を
  芯に埋めない。残りの連続 trait 近似一致・対称サブセット・隣接同型伝播は未着手

## コメント

- 本 doc は `20260725_70` の残作業台帳。設計の根拠は doc 260725201431.md の本文と追記その1〜その19 にある。
- 2026-07-31、doc 260725201431.md の整理に伴い、進捗の未着手 `- [ ]` をここへ発行した。
