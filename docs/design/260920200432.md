---
status: draft # draft|accepted|in-progress|done|superseded|dropped
tags: [refactor, ecs, item, gamedesign] # 領域ラベル
auto: needs-decision # 人の判断必須
---

# prop と item を層で分ける。正のマーカーで同定する

## 背景

フィールドに置かれる物のうち、拾える物すなわちアイテムと、置き物すなわち prop の区別が分かりづらい。原因は同定が**否定**で組まれていることにある。

- `IsPickable` は「フィールド上にいて `Fixed` でない」を拾える条件にする。`internal/world/query/query.go:56`。アイテムを指す正のマーカーは無い。
- FieldLook のカテゴリ表も `Player / Enemy / NPC / Fixed / Tile` で、アイテムのカテゴリが無い。`internal/components/invariant.go:143`。
- prop は `NewPropSpec` が無条件に `Fixed` を付けて作る。`internal/raw/raw.go:695`。分解は `Fixed && GridElement` かどうかで産出先を振り分ける。`internal/activity/disassemble.go:165`。

否定定義は、フィールドに新種の実体が増えるたびに漏れる。実害が #847 で顕在化した。展開キューブが周囲へ出す投影タイル `DeployedTile` は `LocationOnField` を持ち `Fixed` を持たないため、現状の `IsPickable` が **true を返す**。投影タイルを拾えてしまう。`internal/world/lifecycle/cube_deploy.go` の `spawnDeployedTiles`。

## 要件

- アイテムか prop かを**正のマーカー**で同定する。新種の実体が増えても既定で「拾えない」に倒れる。
- ドアや壁のような構造物と、テーブルのような置き家具を、同じ層に整理して正のカテゴリで示す。
- 分解の産出先の振り分けを、`Fixed` の有無でなく層で決める。
- #847 の投影タイルが誤って拾える潜在バグを、同じ設計で塞ぐ。
- 将来: アイテムをフィールドへ置いて家具化する item-deploy と、重い家具を押す/引く移動を、余地として残す。今回は実装しない。

## 設計

### 2軸で捉える。役割層と保存性

フィールドに現れる物を2つの軸で分ける。混同の元は、この2軸を1つの否定フラグに畳んでいたことにある。

- **役割層。何であるか**: `item`（可搬）/ `furniture`（置き家具・構造物）/ `terrain`（壁・床のタイル地形）。
- **保存性。真実か投影か**: 置かれて**保存される**実体か、source から毎フレーム**導出される一時投影**か。`DeployedTile` は後者で、`Deployed` から再導出し serde しない。#847 の `skipComponents` とロード時 despawn がその型。

役割層は存在論、保存性はライフサイクルで、直交する。投影は役割層の存在論には入れない。相互作用も拾得も対象外にする。

### 正のマーカーへ置き換える

`Fixed`（否定の置き物マーカー）を、正のマーカー2つに置き換える。

```go
// 変更前: 置き物を否定で同定する
type Fixed struct{}

// 変更後: 役割層を正のマーカーで同定する
// Item は可搬アイテム。拾える。バックパックへ入る
type Item struct{}

// Furniture は置き家具・構造物。1タイルに1つ。分解でアイテムを産む。既定で拾えない
type Furniture struct{}
```

- アイテムは `Item` を持つ。`NewItemSpec` が付ける。
- prop は `Furniture` を持つ。`NewPropSpec` が付ける。`Fixed` は廃止する。
- 壁は `terrain` すなわち既存のタイル地形のまま。エンティティにしない。
- ドアは開閉状態を持つので `Furniture` の不可搬構造物として entity のまま残す。壁と違い状態があるため terrain 化しない。

### 拾得判定を正へ

```go
// 変更前: フィールド上かつ Fixed でない。投影タイルも誤って true
func IsPickable(entity ecs.Entity, world w.World) bool {
	if !world.Components.LocationOnField.Has(entity) {
		return false
	}
	if world.Components.Fixed.Has(entity) {
		return false
	}
	return true
}

// 変更後: フィールド上かつ Item。Item を持たない投影タイル・ドア・家具は自然に除外
func IsPickable(entity ecs.Entity, world w.World) bool {
	return world.Components.LocationOnField.Has(entity) &&
		world.Components.Item.Has(entity)
}
```

`DeployedTile` は `Item` を持たないので false に倒れる。#847 の潜在バグをこの一手で塞ぐ。

### FieldLook に正のカテゴリを足す

```go
// 変更前
FieldLookCategoryKey: {
	{Name: CategoryPlayer, Pred: c.has(c.Player)},
	{Name: CategoryEnemy, Pred: c.has(c.FactionEnemy)},
	{Name: CategoryNPC, Pred: Or{c.has(c.FactionAlly), c.has(c.FactionNeutral)}},
	{Name: CategoryFixed, Pred: c.has(c.Fixed)},
	{Name: CategoryTile, Pred: c.has(c.Tile)},
},

// 変更後: Item と Furniture の正カテゴリ。Fixed カテゴリは廃止
FieldLookCategoryKey: {
	{Name: CategoryPlayer, Pred: c.has(c.Player)},
	{Name: CategoryEnemy, Pred: c.has(c.FactionEnemy)},
	{Name: CategoryNPC, Pred: Or{c.has(c.FactionAlly), c.has(c.FactionNeutral)}},
	{Name: CategoryItem, Pred: c.has(c.Item)},
	{Name: CategoryFurniture, Pred: c.has(c.Furniture)},
	{Name: CategoryTile, Pred: c.has(c.Tile)},
},
```

### 分解の振り分けを層で決める

```go
// 変更前: Fixed && GridElement で prop 判定
if world.Components.Fixed.Has(target) && world.Components.GridElement.Has(target) {
	// フィールドへ産出し、対象を RemoveEntity
}

// 変更後: Furniture で prop 判定。産出先は層で決まる
if world.Components.Furniture.Has(target) {
	// フィールドへ産出し、対象を RemoveEntity
}
// それ以外は Item として ChangeItemCount(-1)、バックパックへ産出
```

### 1タイル1家具の不変条件

`Furniture` は1タイルに1つに保つ。spawn と将来の item-deploy の直前に `SpatialIndex` で同タイルの `Furniture` 在を確認して弾く。#847 の `deploySpaceFree` が既に「全タイルが空か」を `SpatialIndex` で見ており、同じ仕組みに揃える。`internal/world/lifecycle/cube_deploy.go`。

### #847 との整合。deploy は2種類

- **cube-deploy**（#847）: `Deployed` から `DeployedTile` を撒く投影。保存しない。役割層に属さない。相互作用・拾得の対象外。今回のマーカーを付けない。
- **item-deploy**（将来）: アイテムを1つフィールドへ置いて `Furniture` 化する実体の配置。保存する。1タイル1家具を守る。

名前と挙動を分け、混同を避ける。今回は cube-deploy の投影が拾えない状態を保証するところまでを射程にし、item-deploy 本体は将来フェーズに置く。

## 変更箇所

| ファイル | 変更内容 |
|---|---|
| `internal/components/components.go` | `Fixed` を削除。`Item`・`Furniture` マーカーを追加 |
| `internal/components/genspec/registry.go` | `Item`・`Furniture` を登録、`Fixed` を除去し `make generate` |
| `internal/components/invariant.go` | FieldLook カテゴリを `CategoryItem`・`CategoryFurniture` へ。`CategoryFixed` 廃止 |
| `internal/world/query/query.go` | `IsPickable` を `LocationOnField && Item` へ |
| `internal/raw/raw.go` | `NewItemSpec` に `Item` 付与。`NewPropSpec` は `Fixed` を `Furniture` へ |
| `internal/activity/disassemble.go` | 産出先の振り分けを `Furniture` の有無へ |
| `internal/world/lifecycle/spawn_prop.go` | prop spawn を `Furniture` 前提に。1タイル1家具の確認を足す |
| `internal/save/serde.go` | `Item`・`Furniture` は保存対象。投影の `DeployedTile` は #847 どおり skip |
| `internal/i18n/locale/*.po` | `CategoryItem`・`CategoryFurniture` の表示名 |
| `internal/**/**_test.go` | 拾得・分解・カテゴリ・1タイル1家具のテスト |

## 採用しなかった方法

- **方向 A。prop を重いアイテムへ統一する**: prop を `Item` にして家具は非常に重く移動不能に倒す案。層が1つで済むが、ドアやワープのような構造物まで「重いアイテム」になり、拾得・重量・分解の分岐が結局 prop 専用の重さ判定で復活する。否定判定の入れ替えにしかならず、分かりづらさの根が残る。正の層で分ける方向 B を採る。
- **家具をタイル属性にする**: CDDA 流に家具をタイルの属性で持つ案。1タイル1家具が型で保証されるが、ruins は prop を entity で持つ ECS で、entity から属性へ移すのは spawn・分解・描画・serde の全経路に波及して破壊が大きい。`Furniture` マーカーを付けた entity のまま 1タイル1家具を `SpatialIndex` で守れば、最小の破壊で同じ不変条件に届く。#847 も entity-on-tile で機能している。
- **ドアを terrain タイルへ**: ドアを壁と同じ地形層へ畳む案。CDDA では地形だが、ruins のドアは開閉という状態を持つ。地形をステートフルにする必要が出て、地形は生成の純関数という現在の性質を崩す。ドアは `Furniture` の不可搬構造物として entity に留める。
- **重い家具の押す/引く移動を今回入れる**: 可搬性を item・可搬 / 重家具・押引 / 不可搬の3段にする案。移動コストは既存の `PassCost` の発想を流用できるが、押引は新機構で今回の主眼ではない。MVP は「置く・分解してアイテムに戻す」に絞り、押引は将来フェーズへ。

## コメント

- `Fixed` の全参照を洗い出してから置換する。`grep -rn "\.Fixed\b\|gc.Fixed\|CategoryFixed" internal/`。exhaustive linter が守る散在スイッチがあれば、カテゴリ変更で漏れが lint に出る。
- serde の後方互換は未リリースのため考慮しない。旧セーブに `Fixed` が残っても新コードが無視するだけ。
- #847 が未マージのため、`Item` による `IsPickable` 修正が投影タイルに効くことは #847 マージ後に結合して確認する。順序は #847 を先に入れ、本 doc の変更を後に重ねるのが安全。

## 進捗

- [ ] `Fixed` を削除し `Item`・`Furniture` を追加、登録表更新と `make generate`
- [ ] `IsPickable` を正の判定へ
- [ ] FieldLook カテゴリを `Item`・`Furniture` へ
- [ ] `NewItemSpec`・`NewPropSpec` のマーカー付与を更新
- [ ] 分解の産出先振り分けを `Furniture` へ
- [ ] prop spawn に 1タイル1家具の確認を足す
- [ ] 表示名 po を追加
- [ ] 拾得・分解・カテゴリ・1タイル1家具のテスト
- [ ] `make check` 緑
- [~] item-deploy 本体は将来フェーズ。今回は投影の非拾得保証まで
- [~] 重い家具の押す/引く移動は将来フェーズ
