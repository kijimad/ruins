// Package render3d はタイル世界を透視投影で画面へ写す幾何を提供する。
//
// 責務は座標変換だけに絞る。ECS のタイルやスプライトをどう並べて描くかは呼び出し側が決め、
// この package は「タイルのどの点が画面のどこに来るか」だけを答える。
//
// 世界そのものを描くレンダラと、その上へ重ねるカーソル・エフェクト・HUD が同じ Projector を
// 通ることで、投影先が一致する。変換が2系統に分かれると、片方だけ直して片方が取り残され、
// 指したタイルと違う場所に印が出る。それを型で防ぐのがこの package の存在理由である。
package render3d
