// Package overlay はメニュー画面の上へ重ねる部品を共通化する。
//
// 中心は詳細モーダル Detail で、選択中の対象の名前・説明・性能行をページ送り付きで出す。
// x で開き、左右でページを繰り、Esc・Enter で閉じる。内容は呼び出し側が provide 関数で
// 与えるだけでよく、入力・ページ数・組み立てには触れない。
//
// 重ねる側の契約は ScreenRenderer で、menuloop の Screen が本体を描いたあと、
// RenderOverlay が返すツリーを上へ重ねて描く。
//
// 責務は描画とページ送りに限る。どの操作を実行するかや、どのアイテムを対象にするかといった
// ドメイン判断は呼び出し側の state が持つ。
package overlay
