// Package stage はシームレスワールドの複数ステージ共存を操作するプリミティブを提供する。
//
// 全ステージのエンティティを同一 world に置き、現在ステージだけを稼働させる方式の中核。
// StageBound で所属を、Suspended で稼働可否を表し、ステージ間の往復は SwapTo に集約する。
// query に依存し、その上にステージ単位の退避・再稼働・破棄・束縛を組み立てる。
package stage
